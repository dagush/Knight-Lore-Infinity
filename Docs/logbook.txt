Knight Lore Infinity Logbook
============================

+============================================================+
| 2026 06 28 - Project Foundation and Stage 1                |
+============================================================+

Project framing

Started the Knight Lore / JSSpeccy sliding-window virtualization project. The
core rule was set early and kept strict: the original Knight Lore game code and
3D renderer are not to be modified, and the project must use the original
Spectrum/JSSpeccy display only. All experiments are JavaScript-side emulator
work: memory inspection, static table rewriting, working-memory rewriting, and
runtime pokes/patches.

The initial idea was a 3x3 physical room stencil centered around room 0x88, but
that was refined after reviewing the game transition model. Knight Lore room
movement is cardinal only, so a 5-room cross is enough for the first proof:

- center: 0x88
- west: 0x87
- east: 0x89
- south: 0x78
- north: 0x98

This keeps the project smaller and avoids unnecessary diagonal/corner room
assumptions. The external JavaScript layer will own arbitrary logical map
coordinates, while the original game will continue to see a small physical
neighbourhood.

Planning work

Read and merged the existing overview in Docs/Plan 2026 06 27.txt with the
newer static/dynamic memory findings. Created Docs/Plan 2026 06 28.txt as the
current unified staged plan, while leaving the older plan untouched.

The plan established these important anchors and boundaries:

- location table starts at 0x6251
- room size table is at 0x6248..0x6250
- current room anchor is 0x5C10
- current room attribute candidate is 0x5BAD
- current room size bytes are 0x5BAB, 0x5BAC, and 0x5BAE
- player/body slot starts at 0x5C08
- dynamic object slots are 0x20 bytes each
- slot +8 is a room id
- slot +12 carries transition/entry state bits

The staged plan became:

- Stage 0: project foundation
- Stage 1: read-only room loading anatomy
- Stage 2: prove the physical 5-room cross
- Stage 3 onward: room injection, static table rewrite, working-memory patching,
  recentering, and generalization

------------------------------------------------
Stage 0 - Project Foundation

Set up a safe local working flow around the existing JSSpeccy source tree.

Created and adjusted .gitignore so source documents and important JS/CSS/HTML
files remain trackable, while generated files, local test files, temporary
files, node_modules, dist output, and the Knight Lore .z80 snapshot are ignored.

Created a local _test directory using symbolic links only, so testing can happen
from one simple directory without duplicating source files. The _test directory
links to:

- SRC/static/index.html
- SRC/static/favicon.ico
- SRC/dist/jsspeccy
- Docs/Knight Lore (1984)(Ultimate).z80

Confirmed that a simple Python local server on port 8000 is sufficient for
manual testing.

Configured the emulator page to load the local Knight Lore snapshot by default:

- Knight Lore (1984)(Ultimate).z80

Prepared the JSSpeccy build locally:

- installed local dependencies under SRC/node_modules
- built the JSSpeccy JavaScript and worker bundles
- generated the local dist assets used by _test
- verified the default page and snapshot load from the local server

Stage 0 was considered complete when the emulator loaded the Knight Lore
snapshot successfully from the local testing directory.

------------------------------------------------
Stage 1 - Read-Only Room Loading Anatomy

Added a read-only emulator memory bridge so browser-side JavaScript can inspect
the running Spectrum memory without modifying game memory.

Implemented:

- worker-side readMemory message using the existing WASM core peek(addr)
- host-side readMemory(addr, length) Promise API in JSSpeccy
- host-side frameCompleted event emission after each completed emulated frame
- public pass-throughs for readMemory and on() from the JSSpeccy wrapper
- cache-busting for jsspeccy.js and the worker so diagnostics do not silently
  use stale bundles

Built a Stage 1 diagnostics table directly below the emulator display. The
table is read-only and samples live memory after frameCompleted. It shows:

- sampler status and frame/sample counters
- current location from 0x5C10
- observed secondary room byte from 0x5C30
- player body XYZ
- player head XYZ
- slot +12 flags for the body/head slots
- 0x5BAB, 0x5BAC, 0x5BAD, and 0x5BAE room work bytes
- decoded location table entry for the visible room
- size selector and attribute byte
- background ids
- decoded block/count groups
- decoded packed block positions
- attribute comparison against 0x5BAD
- size selector comparison against 0x5BAB/0x5BAC/0x5BAE
- latest observed room transition
- frameCompleted timing note

During manual inspection, 0x5C10 proved to be the useful current-location byte.
0x5C30 did not reliably update with room transitions, so it was demoted to an
observed-only diagnostic value. The transition detector and location decoder now
use only 0x5C10 as authoritative.

Updated the diagnostics using the detailed location format documented at:

https://www.icemark.com/dataformats/knightlore/index.html

The table now reflects the 0x6251 locations format:

- byte 0: location/screen id
- byte 1: entry size
- byte 2: size selector and attribute
- following bytes: background ids
- 0xFF terminator
- following bytes: block/count groups

Block/count groups are decoded as:

- type = header byte >> 3
- count = (header byte & 7) + 1
- each following position byte is decoded as zz yyy xxx

This was cross-checked against Docs/knightlore.asm, especially retrieve_screen,
which scans the location table, loads backgrounds, and expands foreground block
groups into working graphical object slots.

Important Stage 1 conclusions

- The JavaScript layer can read live emulator memory from the browser side.
- 0x5C10 is the practical current-room/location anchor.
- 0x5C30 is useful to observe, but should not be treated as current-room truth.
- The location table can be decoded live from memory.
- The location entry's attribute and size selector can be compared against
  working-memory fields.
- The frameCompleted boundary is useful for observation, but it is still a
  post-frame boundary. Future recentering may need an earlier worker/core hook
  if invisible patching after a transition proves too late.

Stage 1 was considered complete after the diagnostics table was manually checked
and judged to show the right room-loading information.

Current project state at end of 2026 06 28

- Stage 0 is complete.
- Stage 1 is complete.
- The project has a working local JSSpeccy/Knight Lore test page.
- The emulator can load the Knight Lore .z80 snapshot by default.
- The browser page now includes a reliable read-only memory diagnostics table.
- The next major work should begin Stage 2: prove the 5-room physical cross
  centered on 0x88.

Open cautions

- Do not assume frameCompleted is early enough for mutation stages.
- Do not treat 0x5C30 as authoritative for current room.
- Do not resize location table entries until fixed-capacity rewrite rules are
  proven.
- Keep source-game modifications off limits.
- Keep the original Spectrum display as the only rendering surface.


+============================================================+
| 2026 06 29 - Stage 2 and Stage 3 milestone entry           |
+============================================================+

2026 06 29
----------

This entry covers the work done after the 2026 06 28 logbook entry. The main
result is that Stage 2 and Stage 3 are now complete enough to use as project
milestones.

The emulator-side memory bridge was extended from read-only inspection to
controlled runtime writes:

- worker-side writeMemory message using the existing WASM core poke(addr, byte)
- host-side writeMemory(addr, data) Promise API in JSSpeccy
- public pass-through for writeMemory from the JSSpeccy wrapper

This is still JavaScript-side emulator work only. The original Knight Lore game
source and renderer remain untouched.

------------------------------------------------
Stage 2 - Physical 5-Room Cross

The Stage 2 diagnostics table was added below the Stage 1 table. It tracks the
chosen physical cross:

- center: 0x88
- west: 0x87
- east: 0x89
- south: 0x78
- north: 0x98

The table scans the location table and confirms that retrieve_screen can find
all five physical entries. For each room it shows:

- physical role and room id
- location entry address range
- header byte, size selector, and attribute
- decoded size bytes
- background count
- block/group count
- compact block-group fingerprint
- cardinal arch summary
- diagnostic status

The Counts column was expanded to include general foreground group summaries,
for example:

- 0x05 spike block: 8
- 0x05 spike block: 5
- 0x0D guard square patrol: 1

This makes the physical rooms easier to identify during manual testing without
dumping full block-position details into the Stage 2 table.

Room 0x88 was confirmed to already have four cardinal arch background ids in
its compact entry, so the initial physical center can support all four
directions without needing diagonal/corner rooms. This supports the current
5-room cross model for normal cardinal movement.

Start-room injection was corrected. An initial attempt to poke live room-related
bytes was useful as a probe but not the right initialization path. The proper
source is the start_locations table used by init_start_location. The exact byte
sequence:

- 0x2F, 0x44, 0xB3, 0x8F

was found once in decompressed snapshot RAM at:

- start_locations: 0xD1E2..0xD1E5

The diagnostics now patch that table to:

- 0x88, 0x88, 0x88, 0x88

This makes every randomized start location resolve to physical room 0x88 on the
next game initialization. Importantly, this does not remove 0x5C10 from the
sliding-window model. It only avoids using 0x5C10 as the initialization hack.
0x5C10 remains the important live current-room byte for future transition
detection and renumbering.

Room 0x88 was also cleaned for safer testing (character appearing inside the cauldron).
The compact room entry still contains background ids 0x12 and 0x13, but the expansion
data behind them was zeroed so they become null objects instead of visible
wizard/cauldron elements:

- wizard background data: 0x6FAE..0x6FBE
- cauldron background data: 0x6FBF..0x6FCF
- cauldron bubbles data: 0xB8C8..0xB8D9

This keeps the location entry size and layout intact while removing the wizard,
cauldron, and bubbles from the visible test room. The diagnostics read those
ranges back and report whether they are zeroed.

Stage 2 conclusions

- The 5-room physical cross is visible and decodable from JavaScript.
- retrieve_screen can find all five entries by normal location-table scanning.
- Center room 0x88 has four usable cardinal arch entries.
- No diagonal/corner room is needed for the current cardinal-only transition
  model.
- start_locations at 0xD1E2 is the correct initialization table to patch for
  starting in 0x88.
- 0x5C10 remains reserved for live transition detection and future
  room-renumber pokes, not initial start-room selection.

Stage 2 is considered complete.

------------------------------------------------
Stage 3 - One-Room Injection

Stage 3 proved that the JavaScript layer can rewrite a compact static room
entry while preserving the original entry size, and that the original Knight
Lore room loader will consume the rewritten data when the room is rebuilt.

A dormant one-room injection test harness was added for room 0x88:

- target entry: 0x67BB..0x67CE
- original entry size: 0x13
- normal mode leaves the current clean 0x88 configuration active
- test mode is enabled by opening:
  http://127.0.0.1:8000/index.html?stage3test=1

The test rewrites only bytes inside the existing 0x88 entry. It does not resize
the location table entry. The test fingerprint changes:

- header: 0x06 -> 0x11
- size selector: 0 -> 2
- attribute: 6 -> 1
- decoded size: 64/64/128 -> 64/32/128
- background/wall style: stone arches/walls -> tree-room/wooden style
- foreground group: 0x00 block: 8 -> 0x03 rock: 8

The diagnostics table confirmed the injected compact entry:

- header: 0x11 sel 2 attr 1
- size: 64/32/128
- counts: bg 7; groups 1; 0x03 rock: 8
- arch summary: tree arch entries

The decisive manual visual result was successful: with ?stage3test=1 and a
fresh room build/new game, the Spectrum display showed a wooden room with the
expected altered exits and eight rocks. This proves the original retrieve_screen
path consumed the JavaScript-rewritten static data and built a real game room
from it.

A visible diagnostics/cache marker was added so stale-page problems are easy to
spot:

- diagnostics build: stage3-url-test-20260629-2
- script cache tag: stage2-20260629-6

Normal mode explicitly reports that the Stage 3 one-room injection test is
disabled and explains that ?stage3test=1 enables it. Test mode explicitly
reports that the room 0x88 entry was rewritten while preserving entry size.

Stage 3 conclusions

- Fixed-size static room-entry rewriting works.
- Attribute/colour and size selector rewrites are visible to the decoder.
- Background/wall/arch ids can be rewritten in the compact entry.
- Simple foreground block runs can be rewritten.
- retrieve_screen will build a visibly different room from the rewritten static
  entry when the rewrite happens before the room is built.
- Changing the compact entry after a room is already expanded does not
  necessarily change the visible room immediately; already-expanded working
  memory is a separate mutation target for later stages.

Stage 3 is considered complete.

Current project state at end of 2026 06 29

- Stage 0 is complete.
- Stage 1 is complete.
- Stage 2 is complete.
- Stage 3 is complete.
- The project has runtime memory reads and writes from JavaScript.
- The diagnostics page can prove both physical-cross structure and one-room
  static injection.
- The current default room 0x88 is cleaned of wizard/cauldron visuals.
- The Stage 3 injection test is kept available but opt-in via ?stage3test=1.

Open cautions

- frameCompleted is still only an observation boundary, not yet proven early
  enough for seamless transition mutation.
- Static entry rewrites affect future retrieve_screen builds; immediate visual
  mutation of an already-expanded room requires working-memory patching.
- Keep entry sizes fixed unless/until a safe location-table resizing strategy is
  deliberately designed.
- 0x5C10 must remain part of the future live room-renumbering model.
- Quest-specific hardcoding around the cauldron room should stay out of the
  sliding-window proof until the base transition loop is stable.

================================================
| Logbook entry - 2026 06 30                  |
| Stage 4 and Stage 4.5                       |
================================================

Today started by making one small but important correction to the Stage 3 test
setup. The ?stage3test=1 room injection was changed back to a square 64/64/128
room so the physical 5-room cross can be manually tested by walking from 0x88
to each cardinal neighbour and back. The active test room now uses:

- header 0x06: selector 0, attr 6
- four stone arches: 0x00, 0x01, 0x02, 0x03
- square wall id: 0x0C
- wizard/cauldron ids 0x12 and 0x13 only as null fillers
- foreground fingerprint: 0x03 rock: 8

The room-shape finding was also documented in Docs/info.txt: room dimensions
from 0x5BAB..0x5BAE can correctly report a narrow room while the room looks
square if the static background/wall ids do not match the size selector. This
confirmed that size selector and visual wall/background preset must be treated
as a matched pair.

------------------------------------------------
Stage 4 - Logical Map Model

Stage 4 implemented the first real external logical map model in
SRC/runtime/knightlore.js.

The model now supports:

- integer logical coordinates (x, y)
- coordinate convention: north is y + 1, south is y - 1
- authored room definitions
- deterministic generated rooms for coordinates not explicitly authored
- room payloads with size selector, colour/attribute, exits, backgrounds,
  blocks, objects, and items
- persistent per-room state outside the emulator
- compilation of a logical room into compact Knight Lore location-entry bytes

The browser exposes useful console probes:

- window.KnightLoreInfinity.logicalMap.getRoomAt(12, -4)
- window.KnightLoreInfinity.logicalMap.compileRoomAt(12, -4, 0x88)
- window.KnightLoreInfinity.logicalMap.markVisited(12, -4)

The Stage 4 diagnostics table was added below the emulator. It displays sample
logical queries, including the explicit exit-criteria coordinate (12, -4), and
shows:

- logical coordinate
- room source, authored or generated
- size selector and decoded size
- exits
- background ids
- block summaries
- compiled compact-entry bytes
- persistent room-state revision

The initial Stage 4 generator deliberately produces square rooms. This avoids
rectangular-room long-side door ambiguity while the sliding physical-cross
machinery is still being proven.

Stage 4 conclusion

- JavaScript can ask for a logical room at an arbitrary coordinate such as
  (12, -4).
- The runtime returns a valid room payload.
- That payload can compile into a physical Knight Lore location entry.
- Logical room state exists outside the emulator and survives independently of
  Spectrum memory.

Stage 4 is considered complete as a model/API proof.

------------------------------------------------
Stage 4.5 - Logical Map JSON Authoring

An intermediate Stage 4.5 was added to make the logical map editable outside
the codebase.

Room labels and aliases were added first. A room can now be named with a label
such as:

- 0x88
- cauldron
- start
- west-gallery

The runtime can look up rooms by label or alias:

- window.KnightLoreInfinity.logicalMap.getRoomByLabel("0x88")
- window.KnightLoreInfinity.logicalMap.getRoomByLabel("cauldron")

A JSON map loader was then added. The preferred JSON format is:

- top-level object with format, title, coordinateConvention, origin, notes,
  and rooms
- room entries with label, aliases, coord, size, colour, exits, backgrounds,
  blocks, objects, items, and optional meta

The runtime can load a JSON map from a URL:

- window.KnightLoreInfinity.loadLogicalMapFromUrl("maps/knight-lore-original-map.json")

The test URL for loading the original dump is:

- http://127.0.0.1:8000/index.html?map=maps/knight-lore-original-map.json

The loader was made deliberately strict:

- duplicate logical coordinates are rejected
- duplicate labels are rejected
- duplicate aliases are rejected
- invalid size selectors, colours, background ids, block types, and block
  positions are rejected
- failed loads roll back to the previously loaded map

This strictness is important because JSON files will be edited by hand.

------------------------------------------------
Original Knight Lore JSON Room Dump

A reproducible dump generator was added:

- Tools/dump-original-map.mjs

It reads:

- Docs/knightlore.asm location_tbl

and writes:

- SRC/static/maps/knight-lore-original-map.json

The generated JSON contains all 128 original room/location entries in the same
authoring format used by the runtime. Room order is not important because the
original engine scans the table by room id.

For the dump, the logical coordinate system is centered on original room 0x88:

- 0x88 -> (0, 0)
- 0x89 -> (1, 0)
- 0x87 -> (-1, 0)
- 0x98 -> (0, 1)
- 0x78 -> (0, -1)

The cauldron room is represented as:

- label: 0x88
- alias: cauldron
- originalRoomId: 136

The dump preserves exact original compact background ids and block runs so
users can copy/paste original rooms into custom maps. To keep the editable
format clean, redundant fields were removed from generated room data:

- block type names are documented, not repeated in every block
- raw packed position bytes are not repeated beside decoded x/y/z positions
- theme is omitted when exact backgrounds are present

Original provenance/debug bytes are kept under meta, especially:

- meta.locationEntryAddress
- meta.locationEntrySize
- meta.locationEntryBytes
- meta.foundBackgroundTerminator
- meta.backgroundNames

The foundBackgroundTerminator field records a useful original-format nuance:
some original background-only rooms omit the 0xFF background terminator. The
logical compiler always emits clean semantic entries with a 0xFF separator.

The static build was updated so static/maps is copied into dist/maps. A local
_test/maps symlink was also added so the Python test server can serve:

- maps/knight-lore-original-map.json

------------------------------------------------
Documentation Added

A detailed JSON format document was added:

- Docs/logical-map-json-format.txt

It explains:

- the top-level document shape
- room fields
- coordinate convention
- labels and aliases
- size selectors
- background ids
- block type ids
- compiler layout
- runtime APIs
- original dump notes

This should be the main reference for future-us and for users who want to edit
or build custom maps in a text editor.

------------------------------------------------
Verification

The following checks were run today:

- JavaScript syntax checks for SRC/runtime/knightlore.js
- JavaScript syntax checks for Tools/dump-original-map.mjs
- JSON parse checks for SRC/package.json
- JSON parse checks for SRC/static/maps/knight-lore-original-map.json
- full runtime load of knight-lore-original-map.json
- compilation of all 128 original dumped rooms with zero compile errors
- duplicate-label rejection test
- failed-load rollback test
- webpack rebuild of SRC/dist/jsspeccy/jsspeccy.js
- refreshed SRC/dist/index.html
- refreshed SRC/dist/maps/knight-lore-original-map.json
- local HTTP fetch test for maps/knight-lore-original-map.json
- git diff whitespace check

Current cache/diagnostic marker:

- stage45-json-format-20260630-1

------------------------------------------------
Current project state at end of 2026 06 30

- Stage 0 is complete.
- Stage 1 is complete.
- Stage 2 is complete.
- Stage 3 is complete.
- Stage 4 is complete as a logical-map model/API proof.
- Stage 4.5 is complete as a JSON authoring and original-room dump proof.
- The original game source and renderer remain untouched.
- The JavaScript runtime can read/write emulator memory, decode rooms, patch
  static entries, generate logical rooms, load authored JSON maps, and compile
  logical rooms into physical location-entry payloads.

Open cautions for next time

- Stage 5 should connect the logical model to the physical 5-room cross by
  rewriting all five physical room entries.
- The current JSON loader prepares room payloads, but does not yet inject an
  authored map into the active physical cross automatically.
- Objects and items are accepted and persisted in JS state, but are not yet
  fully compiled into all original runtime tables.
- frameCompleted remains too late or at least risky for seamless recentering;
  the earlier hook/watchpoint question is still open.
- Keep avoiding location-entry resizing until a deliberate safe strategy exists.

End-of-day note

This was a big day for making the project understandable to humans, not just to
the emulator. The map is now something a person can name, edit, copy from, and
reason about.


================================================
| Logbook entry - 2026 07 01                  |
| Stage 5 - Five-Room Cross Injection         |
================================================

Today completed Stage 5. The original goal was to prove that the JavaScript
side can control a small physical room set by rewriting runtime/static map
data, without modifying the original game source and without touching the
renderer.

------------------------------------------------
Room-id force experiment

The first Stage 5 experiment was deliberately tiny: write only the live current
room byte at 0x5C10.

The first version rewrote 0x5C10 back to 0x88 immediately after a drawn frame.
The manual result was surprising and useful:

- leaving room 0x88 did not enter the physical neighbour;
- instead, the game generated room 0x88 on the other side.

This showed that 0x5C10 is not merely a label to normalize after a transition.
It is effectively the room id that retrieve_screen uses when deciding which
static location entry to decode.

The experiment was then generalized into a destination dial:

- ?stage5force=0x78
- ?stage5force=0x98
- ?stage5force=0x87
- ?stage5force=0x89
- ?stage5force=0x88

The force-dial logic:

- waits until the game is stably in 0x88;
- reads 0x5C10 after each drawn frame;
- skips writes while 0x5C10 is 0x88;
- skips writes while 0x5C10 is already the forced target;
- otherwise writes only 0x5C10 to the forced target room id.

Manual testing with ?stage5force=0x78 confirmed the model:

- exiting 0x88 in any direction entered/generated 0x78;
- exiting 0x78 north naturally returned to 0x88, because 0x88 is explicitly
  skipped by the force logic;
- exiting 0x78 in other directions forced the destination back to 0x78;
- the player appeared on the correct side of the destination room.

This is strong evidence that transition positioning and room generation can be
controlled separately enough for sliding-window virtualization.

------------------------------------------------
3x3 Static Map Fixture

A small authored static-map fixture was generated in the existing logical-map
JSON format:

- SRC/static/maps/knight-lore-3x3-static-map.json

The requested square was centered on original room 0x88:

- top row:    0x97, 0x98, 0x99
- middle row: 0x87, 0x88, 0x89
- bottom row: 0x77, 0x78, 0x79

Original room 0x99 does not exist in the dumped location table, so the fixture
contains eight authored rooms rather than nine.

For consistency, every exit/background arch that would point outside the
authored room set was removed. This also avoids any door pointing into the
missing 0x99 coordinate.

Room 0x88 in the fixture is the cleaned test-room version:

- four cardinal exits;
- original block data;
- no wizard or cauldron background ids 0x12/0x13.

The generated fixture was validated by loading it through the runtime logical
map loader and compiling all eight rooms successfully.

------------------------------------------------
Static Location Table Erase/Inject

An opt-in static-table injection path was added:

- http://127.0.0.1:8000/index.html?stage5static3x3=1

When enabled, the JavaScript side:

1. fetches maps/knight-lore-3x3-static-map.json;
2. compiles each authored room into original Knight Lore compact location-entry
   bytes;
3. erases the original location table range 0x6251..0x6BD0;
4. writes the compact injected table back starting at 0x6251.

Stage 3 fixed-address one-room injection is suppressed while this static-map
injection is active, because the original 0x88 entry address is no longer safe
after the table is erased and rewritten compactly.

The diagnostics table gained a row:

- Stage 5 static map injection

The row reports the loaded map, number of injected rooms, original table range,
compiled byte count, and per-room entry sizes.

After the final fix, the injected table reports:

- Injected 8 rooms
- 157/2432 compiled bytes
- 0x97:15 0x98:25 0x87:23 0x88:17 0x89:19 0x77:5 0x78:24 0x79:21

Manual testing showed that the game can run on this erased-and-rewritten compact
static map.

------------------------------------------------
Important 0xFF Background-Only Room Rule

During manual testing, entering room 0x77 reset/crashed the emulator before the
room was shown. This revealed a critical original-data rule.

Original room 0x77 has no foreground block data and its original compact entry
does not contain a 0xFF separator. The first compiler version always emitted
0xFF after the background list. For a background-only room this produced:

- id, size, header, backgrounds..., 0xFF

with no block/count data after the 0xFF. In retrieve_screen, that makes the
engine enter the foreground-object decoder with no foreground bytes left. It
can then read the next room id as block data, which is exactly the kind of
fragile data mistake that can crash or reset the game.

The compiler rule is now:

- if foreground block groups exist:
  id, entrySize, header, backgrounds..., 0xFF, block/count groups...
- if no foreground block groups exist:
  id, entrySize, header, backgrounds...

Room 0x77 now compiles as:

- 77 05 03 00 01 0C

This rule was documented in:

- Docs/info.txt
- Docs/logical-map-json-format.txt

------------------------------------------------
Files and Runtime Pieces Added/Changed

Main runtime work:

- SRC/runtime/knightlore.js
  - Stage 5 force-destination experiment
  - static JSON map compilation to compact location entries
  - location-table erase/inject path
  - background-only room compiler fix

- SRC/runtime/jsspeccy.js
  - memory write helpers that can report previous values
  - conditional write helper used by the room-id force experiment

- SRC/runtime/worker.js
  - worker-side conditional read-previous/write command

- SRC/static/index.html
  - diagnostics/cache marker updates

New fixture:

- SRC/static/maps/knight-lore-3x3-static-map.json

Documentation:

- Docs/info.txt
- Docs/logical-map-json-format.txt

------------------------------------------------
Verification

Checks run during this stage included:

- JavaScript syntax checks for SRC/runtime/knightlore.js
- JavaScript syntax checks for SRC/runtime/jsspeccy.js
- JavaScript syntax checks for SRC/runtime/worker.js
- runtime load/compile checks for the 3x3 JSON fixture
- validation that no authored fixture exit points outside the authored room set
- validation that 0x77 compiles without 0xFF
- webpack rebuild of SRC/dist/jsspeccy/jsspeccy.js
- refreshed SRC/dist/index.html
- refreshed SRC/dist/maps/knight-lore-3x3-static-map.json
- local browser smoke tests for:
  - ?stage5force=0x78
  - ?stage5static3x3=1
- git diff whitespace checks

Current cache/diagnostic marker:

- stage5-static-map-20260701-1

------------------------------------------------
Stage 5 conclusion

Stage 5 is complete.

The important proof is no longer only that we can rewrite one room. We have now
shown that:

- 0x5C10 can choose the next room generated by retrieve_screen;
- transition-side positioning still works while forcing destinations;
- the original location table can be erased at runtime;
- a compact authored static table can be written in its place;
- the original engine can scan and decode that injected table;
- background-only entries must follow the original no-0xFF convention.

This validates the core sliding-map assumption: while the player is inside a
room, the original static map can be considered disposable. It only needs to be
valid when the engine leaves the current room and scans for the next location.

Next natural step: use the same injection machinery to rewrite the physical
window whenever the logical coordinate changes.


+============================================================+
| 2026 07 03 - Stage 6, Stage 7, and Procedural Map          |
+============================================================+

2026 07 03
----------

This entry covers the work after the Stage 5 static-table injection milestone.
The project moved from "we can erase and rewrite the map" to a working
four-direction sliding-window proof with an unbounded deterministic logical
map provider.

The important rule stayed unchanged: no original Knight Lore source code was
modified, and no alternate renderer was added. The original Spectrum/JSSpeccy
display remains the only visual output. All map virtualization is still done
from JavaScript by rewriting the emulator's runtime memory and static location
table.

------------------------------------------------
Stage 6 - One-Direction Recenter Proof

Stage 6 was considered already proven by the earlier 0x5C10 force-destination
experiments.

The key observation was that changing only the current-room byte after a room
has been drawn can control which room retrieve_screen loads on the next
transition. When the player leaves the current room, the original engine scans
the location table for the room id in 0x5C10, decompresses that location entry
into working memory, and then resumes play from the correct transition side.

Manual testing confirmed the expected behaviour:

- leaving physical 0x88 in any direction can force the next generated room;
- entering a forced destination still places the player on the correct side;
- forcing can be skipped for selected rooms, allowing natural transitions back
  to those rooms.

This established that transition positioning, current-room selection, and
static-table content are separable enough for a recentering model.

------------------------------------------------
Stage 7 - Four-Direction Sliding Cross

Implemented the first full sliding physical cross.

The physical room ids remain fixed:

- center: 0x88
- west: 0x87
- east: 0x89
- south: 0x78
- north: 0x98

The logical map is external and coordinate-based. The runtime tracks a logical
center coordinate, initially (0, 0). Whenever 0x5C10 shows that the original
engine has entered one of the four physical neighbour rooms, the JavaScript
side:

1. detects the transition direction from the physical room id;
2. updates the logical center coordinate;
3. compiles the new center plus its four logical neighbours;
4. erases and rewrites the physical five-room location table entries;
5. patches active working-memory room-id bytes from the entered physical
   neighbour back to 0x88.

This makes the original game continue as if the player is always centered
around physical room 0x88, while the external logical coordinate keeps moving.

Manual movement tests covered:

- repeated movement in one direction;
- east/east/west/west style backtracking;
- north/south alternation;
- small loops;
- alternating axes.

The first generated maps exposed a real authoring/runtime problem: a visible
door must have a corresponding return door in the contiguous logical room. If
not, Knight Lore can transition into a room whose wall/background data has no
matching entrance side, leaving the player behind a wall. A reciprocal-exit
overlay was added at compile time. The source logical map is not modified, but
the compiled physical payload is made safe:

- authored exits are mirrored when both room shapes can legally host them;
- generated-to-generated exits are reciprocal by construction;
- generated rooms do not reopen authored map edges on their own;
- illegal rectangular long-side exits are closed rather than emitted.

After this fix, repeated manual walking in all directions worked without the
wall-lock problem.

------------------------------------------------
Logical Map Model and Authored JSON

The logical map model now supports authored rooms, labels, aliases, generated
fallback rooms, and persistent room state outside the emulator.

Important pieces:

- SRC/runtime/knightlore.js owns the logical map facade and Stage 7 physical
  cross compiler.
- SRC/static/maps/knight-lore-original-map.json contains a JSON dump of the
  original Knight Lore room set.
- Docs/logical-map-json-format.txt documents the editable JSON format.

Labels were added so authored rooms can be referred to by names such as
"0x88" or "cauldron". The original map dump gives users a copy/paste source
for building new maps from original rooms, while the runtime still treats room
coordinates as the real addressing system.

The JSON documentation was expanded with:

- finite versus infinite map notes;
- reciprocal-exit safety rules;
- starting-room clearance requirements;
- original-game rectangular room size rules;
- safe wooden-room background combinations;
- the critical background-only no-0xFF compiler rule.

------------------------------------------------
Procedural Infinite Map Provider

Added a dedicated procedural map generator:

- SRC/runtime/knightlore-mapgen.js

The generator answers "what room exists at logical coordinate (x, y)?" without
requiring a finite map array. Its answers are deterministic: the same world
seed and coordinate always produce the same room.

The first version split the plane into deterministic chunks, carved guaranteed
paths between shared edge connectors, and added branch rooms. This proved the
infinite-map idea, but manual play showed the early generator produced too many
dense square/T/four-way areas and too few rectangular rooms.

The generator was then tuned against the original Knight Lore map:

- original occupancy: 128 rooms in a 16x16 map = 50%;
- original rectangular rooms: 58 out of 128 rooms = 45.31%.

The current algorithm:

- uses deterministic 8x8 chunks;
- chooses matching cross-chunk connector positions from shared edge hashes;
- carves paths from the four edge connectors to a deterministic hub;
- forces the origin and its four cardinal neighbours to exist;
- grows deterministic corridor branches until local occupancy is near 50%;
- derives exits from neighbouring-room existence;
- returns missing cells as closed wall-only payloads;
- creates original-style rectangular rooms only when exits are on the smaller
  side(s) of the rectangle;
- uses a deterministic throttle so not every legal rectangle candidate becomes
  rectangular.

The latest measured sample was close to the original proportions:

- generated occupancy: about 49.84%;
- generated rectangular rooms: about 45.12%.

Rectangular generated rooms are intentionally conservative for now:

- selector 1 for north/south-side candidates;
- selector 2 for east/west-side candidates;
- no long-side doors;
- stone-only;
- no procedural block runs.

Square generated rooms may be stone or wooden. Procedural foreground blocks
avoid the central starting floor cells, so a generated origin room does not
trap the player immediately.

A separate explanation was added:

- Docs/map-generation-algorithm.txt

------------------------------------------------
Verification

Checks and probes run during this stage included:

- JavaScript syntax checks for SRC/runtime/knightlore.js;
- JavaScript syntax checks for SRC/runtime/knightlore-mapgen.js;
- generated-map reciprocal-exit probes;
- rectangular-room legality probes;
- generated occupancy and rectangle-density measurements against the original
  map dump;
- reachability probes with an outer margin and inner target window;
- manual walking tests in all directions;
- webpack rebuilds of SRC/dist/jsspeccy/jsspeccy.js;
- refreshed SRC/dist/index.html.

Current cache/diagnostic marker:

- stage7-balanced-rectangles-20260704-1

------------------------------------------------
Stage 7 status

Stage 7 is working as a practical proof.

The original engine sees only a fixed five-room physical cross, while the
JavaScript side keeps an arbitrary logical coordinate system, rewrites the
physical room entries around the new center, and keeps the player effectively
centered on 0x88.

Known cautions remain:

- all timing still depends on patching at a safe point around room loading;
- more complex objects/items are not yet persistent across revisits;
- generated rectangular rooms are deliberately simple until more original
  rectangular/background patterns are tested;
- authored special rooms still need careful spawn and reciprocal-exit checks.

The next natural work is content enrichment: hazards, treasures, authored
special rooms, persistent object/item state, and more varied room dressing.


+============================================================+
| 2026 07 06 - Wooden Rectangular Rooms and Wall Patterns    |
+============================================================+

2026 07 06
----------

This entry covers the Stage 7 content/dressing work after the deterministic
sliding-cross proof. The main goal was to make generated wooden/tree regions
visually coherent without changing the original renderer or original game
source. Everything remains JavaScript-side emulator work: static location table
rewriting, custom background data in emulator memory, and diagnostics below the
JSSpeccy display.

------------------------------------------------
Biome / Style Decisions

The meaning of "biome/style decisions" was clarified. In this project it does
not mean a new renderer, modern graphics, or external scene drawing. It means a
room-dressing policy that still compiles into original Knight Lore compact
location data:

- generated labels and titles;
- procedural metadata;
- room colour/attribute;
- stone versus tree theme;
- simple block dressing for square rooms;
- rectangular room shape selection where original Knight Lore rules allow it.

A Stage 7 "Current Generated Cross" diagnostics table was added immediately
below the emulator. It shows the five logical rooms currently compiled into the
physical cross:

- physical role and room id;
- logical coordinate;
- generated/authored label;
- biome/style region;
- size selector and decoded dimensions;
- theme and colour;
- exits;
- block summary;
- compact entry/background summary.

This made it possible to see when a room was logically wooden-wing but still
visually stone because rectangular tree-wall data was not yet available.

------------------------------------------------
Rectangular Room Rules

The generator keeps the original Knight Lore rectangular-room legality rule:

- selector 1 is 32x64x128 and only allows north/south exits;
- selector 2 is 64x32x128 and only allows east/west exits;
- long-side doors are not emitted;
- corners, T-junctions, and four-exit rooms remain square.

Generated rectangular rooms still have no procedural block runs. This keeps the
shape experiment isolated from collision and object persistence issues.

The rectangle density was tuned against the original dump:

- original rectangular rooms: 58 / 128 = 45.31%;
- generated rectangular rooms remain close to this ratio after deterministic
  throttling of legal rectangular candidates.

------------------------------------------------
Wooden Rectangular Room Problem

The original game appears to use the tree/wood backgrounds 0x0F, 0x10, and
0x11 only with square rooms. Rectangular wooden rooms therefore needed custom
runtime background definitions.

Stage 7 already erases and rewrites the original location table. The custom
rectangular tree-wall experiment uses spare space in the unused tail of that
erased table and repoints two background ids at runtime:

- 0x12: custom tree wall size 2, for selector 2 rooms;
- 0x13: custom tree wall size 3, for selector 1 rooms.

This is Stage 7 runtime data only. In the original game data, 0x12 and 0x13
are wizard and cauldron backgrounds. Authored maps that need those original
visuals must not rely on those ids while the Stage 7 custom wall experiment is
active.

The first stable custom tree-wall pass kept only source wall records already
using Z/base byte 0x80. Raised stone-wall layers were discarded rather than
moved down. Moving raised records to ground level could cover valid arches or
create unstable room layouts.

Several unsafe/insufficient attempts were observed and reverted:

- grounding all raised stone wall records covered exits;
- hand-filling long walls with new explicit records caused player spinning and
  emulator hangs;
- copying only a few extra middle records was also unstable;
- using only the two square-room filler records avoided hangs but left visible
  holes in the long doorless walls.

------------------------------------------------
Diagnostics-Driven Wall Tuning

A "Wooden Square Wall Reference" table was added above the Stage 7 cross
diagnostics. It lists the original square wooden wall records from:

- 0x0F tree room size 1;
- 0x10 tree filler west;
- 0x11 tree filler north.

This gave a direct byte-level reference for the safe square-tree wall pattern.

A slider probe was then added for the selector 1 west wall X coordinate. The
initial question was whether the two 0x10 filler-west rows should remain at
the original square-room X 0x3F or move to the translated rectangular wall
plane. Manual testing found that X 0x5F is the correct value for selector 1.

The diagnostics table was then expanded to show the actual live custom wall
records being injected. This made it clear whether a wall segment was missing
from the payload or merely hidden/occluded in the Spectrum view.

------------------------------------------------
Final Custom Tree-Wall Patterns

The final rule is not to append a few filler rows over the original
stone-derived wall. Instead, remove the matching stone-derived ground wall
records from the custom background and replace them with the full translated
8-row square-tree wall pattern.

Selector 1 / custom 0x13 uses the full square-tree west wall pattern translated
to x=0x5F:

    0x80 0x5F 0x49 0x80 0x00 0x08 0x2C 0x10
    0x81 0x5F 0x58 0x80 0x00 0x08 0x2C 0x10
    0x82 0x5F 0x68 0x80 0x00 0x08 0x2C 0x10
    0x80 0x5F 0x78 0x80 0x00 0x08 0x2C 0x10
    0x81 0x5F 0x88 0x80 0x00 0x08 0x2C 0x10
    0x80 0x5F 0x98 0x80 0x00 0x08 0x2C 0x10
    0x81 0x5F 0xA8 0x80 0x00 0x08 0x2C 0x10
    0x82 0x5F 0xB8 0x80 0x00 0x08 0x2C 0x10

Selector 2 / custom 0x12 uses the full square-tree north wall pattern
translated to y=0xA0:

    0x80 0x48 0xA0 0x80 0x08 0x00 0x2C 0x50
    0x81 0x58 0xA0 0x80 0x08 0x00 0x2C 0x50
    0x82 0x68 0xA0 0x80 0x08 0x00 0x2C 0x50
    0x80 0x78 0xA0 0x80 0x08 0x00 0x2C 0x50
    0x81 0x88 0xA0 0x80 0x08 0x00 0x2C 0x50
    0x80 0x98 0xA0 0x80 0x08 0x00 0x2C 0x50
    0x81 0xA8 0xA0 0x80 0x08 0x00 0x2C 0x50
    0x82 0xB8 0xA0 0x80 0x08 0x00 0x2C 0x50

Manual testing confirmed:

- selector 1 north/south-axis rectangular wooden rooms now look correct;
- selector 2 east/west-axis rectangular wooden rooms were fixed by the mirrored
  0x12 north-wall replacement;
- walking through the generated map remains stable after the wall-pattern
  change.

The diagnostics page now shows 16 live custom wall rows:

- 8 rows for custom 0x12;
- 8 rows for custom 0x13.

------------------------------------------------
Files and Documentation Updated

Runtime/frontend:

- SRC/runtime/knightlore.js
  - custom rectangular tree-wall builders;
  - full 8-row translated west-wall pattern for 0x13;
  - full 8-row translated north-wall pattern for 0x12;
  - filtering of matching stone-derived ground wall records before appending
    translated tree records;
  - Stage 7 diagnostics for the live custom wall rows.

- SRC/static/index.html
  - Wooden Square Wall Reference table;
  - selector 1 west-wall X slider, defaulting to 0x5F;
  - live custom 0x12/0x13 wall-record diagnostics.

Documentation:

- Docs/info.txt
  - quick-reference note updated from the earlier filler-only strategy to the
    full 8-row custom wall patterns.

- Docs/logical-map-json-format.txt
  - rectangular tree-room format caveat updated with the exact 0x12 and 0x13
    custom wall records and the "replace, do not append" rule.

- Docs/map-generation-algorithm.txt
  - procedural wooden-wing rectangular room notes updated to describe the final
    custom wall strategy.

------------------------------------------------
Verification

Checks and probes run during this work included:

- JavaScript syntax checks for SRC/runtime/knightlore.js;
- git diff whitespace checks for edited runtime/static files;
- webpack rebuilds of SRC/dist/jsspeccy/jsspeccy.js;
- refreshed SRC/dist/index.html;
- browser smoke tests for the diagnostics page;
- checks that the live diagnostics table shows 8 custom 0x12 rows and 8 custom
  0x13 rows;
- manual walking tests through rectangular wooden rooms in both orientations.

Current cache/diagnostic marker:

- stage7-full-rect-walls-20260706-1

------------------------------------------------
Current project state at end of 2026 07 06

- Stage 7 remains a working sliding physical-cross proof.
- Generated wooden/tree regions now support rectangular rooms in both legal
  orientations.
- Rectangular wooden rooms are still Stage 7 runtime-injected data, not
  original 1984 stock data.
- The original renderer and original game source remain untouched.
- Objects/items and richer content are still future work.
- The next natural content work can move on to treasures, hazards, authored
  special rooms, and persistent state now that the basic room shape/style layer
  is less visually brittle.


+============================================================+
| 2026 07 08 - Stage 8 Dispersed Quest Sector Model          |
+============================================================+

2026 07 08

Stage 8 changed the infinite map from a room-generation proof into the first
layer of a new dispersed quest structure.

The core idea is that each 8x8 logical sector can own a local quest:

- a local cauldron anchor room;
- a local charm anchor room;
- one required charm id;
- persistent JavaScript-side quest state.

This is deliberately metadata-only for the first proof. No original charm,
cauldron, wizard, inventory, or cauldron-order memory is modified yet.

------------------------------------------------
Planning Update

A new plan file was added:

- Docs/Plan 2026 07 08.txt

The plan defines "Dispersed Quest Sectors and External Dialogue" as the next
major layer after the Stage 7 sliding cross. It records the idea that Knight
Lore Infinity should not only be a larger map, but a world made from local
quest regions.

The plan was also corrected after discussion:

- the cauldron and charm belong to the same 8x8 sector;
- the route between them does not have to stay inside that sector;
- detours through neighbouring sectors are valid and desirable;
- fairness should mean global traversable-world reachability, not isolated
  sector-local reachability.

This reduces pressure on the generator and makes neighbouring sectors feel more
integrated.

------------------------------------------------
Generator Changes

Stage 8 quest metadata was added to:

- SRC/runtime/knightlore-mapgen.js

The procedural map generator now exposes deterministic sector queries:

- getQuestSectorAt(x, y)
- getQuestRoomInfoAt(x, y)

Each sector currently uses the existing 8x8 chunk grid and contains a quest by
default. The metadata includes:

- sector coordinate;
- sector origin;
- local room coordinate inside the sector;
- style/biome information;
- required charm type. This was originally written as 14 abstract labels, but
  later C3.2 evidence corrected the model: Knight Lore has seven quest charm
  types, requested twice;
- cauldron anchor coordinate;
- charm anchor coordinate;
- difficulty value;
- placement note.

The first implementation chooses cauldron/charm anchors from already existing
connected cells in the chunk:

- cauldrons are biased toward the chunk hub;
- charms are biased farther from the cauldron;
- if both anchors collide, another existing cell is chosen when available.

Anchor rooms are marked as existing cells, so the metadata never points at a
missing generated room.

Important correction:

- the old central floor-cell block avoidance rule now applies only to the true
  logical start room (0, 0);
- other rooms may use those central cells, though entrance strips still need to
  remain safe in future content-generation work.

------------------------------------------------
Runtime and State Changes

The logical map wrapper in:

- SRC/runtime/knightlore.js

now attaches quest metadata to logical room payloads:

- questRole:
  - none;
  - cauldron;
  - charm;
- questSector;
- questCharm;
- meta.quest.

A separate persistent sector-state map was added outside the emulator. Sector
state currently tracks:

- unvisited;
- cauldronSeen;
- charmSeen;
- charmCollected;
- completed;
- status;
- revision.

For this first proof, visiting a logical room marks only JavaScript-side state.
The original Spectrum game memory is not touched for quest state.

The public browser API now includes:

- window.KnightLoreInfinity.getQuestSector(x, y)
- window.KnightLoreInfinity.getQuestRoomInfo(x, y)
- window.KnightLoreInfinity.logicalMap.getQuestSectorAt(x, y)
- window.KnightLoreInfinity.logicalMap.getQuestRoomInfoAt(x, y)

This satisfies the Stage 8 exit criterion that JavaScript can ask what quest
sector contains a coordinate such as (12, -4), what charm is required, and
where the cauldron/charm anchors are.

------------------------------------------------
Diagnostics

A new diagnostics table was added immediately below the emulator display in:

- SRC/static/index.html

The new section is:

- Stage 8 - Dispersed Quest Sector Model

It displays:

- current logical coordinate;
- current sector and local coordinate;
- current room quest role;
- required charm;
- cauldron anchor coordinate;
- charm anchor coordinate;
- persistent sector state;
- API probe for coordinate (12, -4).

The cache/diagnostic marker was updated to:

- stage8-quest-sector-20260708-1

The Stage 8 table is intentionally external HTML diagnostics only. It does not
draw anything inside the Spectrum display.

------------------------------------------------
Manual Tests

The first manual sector test was in sector (0, 0).

Observed table state:

- current room reached: (3, 0);
- sector: (0, 0);
- required charm: charm 11;
- cauldron anchor: (4, 4);
- charm anchor: (3, 0);
- state: cauldron seen and charm seen.

The player successfully travelled:

- from (0, 0) to the cauldron at (4, 4);
- then to the charm at (3, 0);
- without room-transition instability.

A second manual test checked a negative-coordinate sector.

Observed table state:

- current room reached: (0, -5);
- sector: (0, -1);
- local coordinate: (0, 3);
- required charm: charm 12;
- cauldron anchor: (6, -8);
- charm anchor: (0, -5);
- state: cauldron seen and charm seen.

This confirmed that negative-coordinate floor division/modulo still behaves
properly in the quest-sector layer.

The API probe also reported an independent sector:

- coordinate (12, -4);
- sector (1, -1);
- required charm: charm 01;
- cauldron anchor: (15, -6);
- charm anchor: (8, -4).

This shows multiple sector states can coexist independently.

------------------------------------------------
Verification

Automated/local checks included:

- JavaScript syntax checks for:
  - SRC/runtime/knightlore-mapgen.js;
  - SRC/runtime/knightlore.js;
- git diff whitespace checks;
- webpack rebuild of the JSSpeccy bundle;
- refreshed SRC/dist/index.html from SRC/static/index.html;
- browser smoke test at:
  - http://127.0.0.1:8000/index.html?stage7sliding=1

The browser smoke test confirmed:

- the Stage 8 table rendered;
- the table showed four rows:
  - current;
  - cauldron;
  - charm;
  - API probe;
- getQuestSector(12, -4) returned sector (1, -1);
- the API probe reported that a quest exists;
- no page errors were captured.

The webpack build still reports only the existing bundle-size warnings.

------------------------------------------------
Current project state at end of 2026 07 08

- Stage 8 is complete as a metadata proof.
- The infinite map now has deterministic local quest-sector metadata.
- Cauldron/charm anchors are logical metadata only.
- Persistent quest state lives outside the emulator.
- Manual testing confirmed two sectors, including a negative-coordinate sector.
- No original game source, original renderer, or original quest memory has been
  modified.
- The next natural step is Stage 8.1: automated/global reachability checks for
  cauldron and charm anchors, so fairness is tested by code rather than by
  manual wandering.


+============================================================+
| 2026 07 09 - Stage 8.1 Reachability Diagnostics            |
+============================================================+

2026 07 09

Stage 8.1 added an explicit reachability diagnostic for the dispersed quest
sector model.

The important design clarification today was about charms:

- a charm type is global;
- the local sector charm is a nearby fair source;
- a cauldron requesting that charm type should accept it even if the player
  collected it in another sector;
- the generator must therefore not enforce "same-sector charm only" as a rule.

This keeps the quest model closer to the spirit of Knight Lore: the player can
make inefficient or surprising routing choices, but the game should not forbid
them merely because they cross sector boundaries.

------------------------------------------------
Reachability Implementation

The procedural generator now exposes:

- getQuestReachabilityAt(x, y)

The reachability check:

- finds the quest sector containing the queried coordinate;
- confirms that the cauldron anchor room exists;
- confirms that the charm anchor room exists;
- runs a bounded breadth-first search over the generated room graph;
- reports whether the cauldron and charm anchors are in the same traversable
  component inside the bounded search area.

The search uses the same room-existence graph that normal generated exits use.
If room A exists next to room B, the generated exit overlay can expose the
corresponding reciprocal doorway.

The default search margin is:

- questReachabilityMarginChunks: 2

The diagnostic reports:

- anchor existence;
- reachable yes/no;
- path length;
- visited rooms;
- searched rooms;
- bounded search margin;
- a small path sample for API inspection.

------------------------------------------------
Diagnostics and API

The diagnostics page was updated from:

- Stage 8 - Dispersed Quest Sector Model

to:

- Stage 8.1 - Sector Reachability Guarantee

The table now includes a "Reach" column. A healthy sector reports values like:

    OK; anchors CH; path 7; searched 39; margin 2 chunk(s)

Where:

- C means the cauldron anchor exists;
- H means the charm anchor exists;
- path is the cauldron-to-charm path length;
- searched is the number of graph nodes expanded by the bounded BFS.

The public browser API now includes:

- window.KnightLoreInfinity.getQuestReachability(x, y)
- window.KnightLoreInfinity.logicalMap.getQuestReachabilityAt(x, y)

The cache/diagnostic marker was updated to:

- stage8-reachability-20260709-1

------------------------------------------------
Verification

Automated checks:

- JavaScript syntax checks passed for:
  - SRC/runtime/knightlore-mapgen.js;
  - SRC/runtime/knightlore.js;
- git diff whitespace checks passed;
- webpack rebuild passed with only the existing bundle-size warnings;
- browser smoke test passed at:
  - http://127.0.0.1:8000/index.html?stage7sliding=1

The browser smoke test confirmed:

- the Stage 8.1 table rendered;
- the new Reach column was present;
- the current sector reported reachability OK;
- getQuestReachability(12, -4) reported:
  - cauldron exists;
  - charm exists;
  - reachable true;
  - path length 11.

A wider generator scan checked 81 sectors:

- sector range: (-4, -4) through (4, 4);
- failures found: 0;
- maximum observed cauldron/charm path length: 13;
- maximum observed visited rooms in a reachability check: 159.

Manual testing also crossed several sectors without observing a reachability
failure.

------------------------------------------------
Important Honesty Note

The current result is a strong practical guarantee, not a formal mathematical
proof over the infinite map.

Why confidence is high:

- every chunk carves all four edge connectors to its hub;
- neighbouring chunks derive shared edge connector positions from the same
  deterministic seeds;
- quest anchors are chosen from existing generated cells and marked as existing;
- the reachability check uses the same generated room graph as normal exits;
- automated sector sampling and manual exploration found no failures.

What is not proven:

- no finite scan, whether 81 sectors or one million sectors, can prove by
  itself that an infinite deterministic map has no possible reachability
  failure;
- a full proof would require mathematical reasoning about all possible chunk
  and neighbour combinations, which is outside the goals of this project.

Therefore the correct project statement is:

- reachability failures are very unlikely under the current generator rules;
- the active sector can be checked directly by diagnostics;
- the implementation is sufficient for the project stage, but it is not a
  theorem about the infinite plane.

------------------------------------------------
Files Updated

- SRC/runtime/knightlore-mapgen.js
  - bounded cached quest reachability search;
  - getQuestReachabilityAt API;
  - reachability stats.

- SRC/runtime/knightlore.js
  - logical-map wrapper for reachability;
  - Stage 8.1 diagnostics formatting;
  - public browser API;
  - global-charm policy note in quest metadata.

- SRC/static/index.html
  - Stage 8.1 title;
  - Reach column;
  - updated cache marker.

- Docs/Plan 2026 07 08.txt
  - global-charm clarification;
  - bounded diagnostic/non-proof caveat.

- Docs/map-generation-algorithm.txt
  - quest-sector layer documentation;
  - reachability diagnostic documentation;
  - empirical confidence and non-proof warning.

------------------------------------------------
Current project state at end of 2026 07 09

- Stage 8.1 is complete as a practical reachability diagnostic.
- Quest-sector anchor rooms are checked for existence and bounded
  traversability.
- The player is not forced to use the charm from the same sector as the
  cauldron.
- The system remains JS-side only: no original source, renderer, item slots, or
  cauldron logic have been modified.
- The next natural stage is visual/semantic dressing of cauldron and charm
  rooms, still conservatively and without touching original item mechanics.


+============================================================+
| 2026 07 13 - Stage 8.2 Quest Room Dressing                 |
+============================================================+

2026 07 13

Stage 8.2 made cauldron and charm anchor rooms identifiable without touching
the original Knight Lore quest machinery.

The stage was split into two deliberately conservative phases:

- Phase A: diagnostics-only semantic dressing;
- Phase B: safe static room-entry dressing.

------------------------------------------------
Phase A - Diagnostics-Only Dressing

Phase A added quest-room intent to the logical payload:

- questDressing metadata;
- distinct generated labels:
  - quest-cauldron:x,y:biome-id;
  - quest-charm:x,y:biome-id;
- diagnostic table formatting for the intended role/profile.

The diagnostics table was updated to show the dressing intent for:

- current room;
- current sector cauldron anchor;
- current sector charm anchor;
- fixed API probe.

No original room bytes, object records, item slots, cauldron state, wizard
state, inventory state, or charm-order memory were changed in Phase A.

------------------------------------------------
Phase B - Safe Static Room-Entry Dressing

Phase B made the quest anchor rooms visibly different inside the original
Spectrum/JSSpeccy display while still avoiding the fragile original quest
logic.

The rule is intentionally boring and engine-friendly:

- generated cauldron/charm anchor rooms are forced to square selector 0;
- only compact location-entry fields are changed:
  - header colour/attribute;
  - foreground block runs;
- normal generated wall/arch backgrounds are still used for exits;
- no original cauldron/wizard ids are used as quest objects;
- no original collectable item slots, object-table quest records, inventory
  bytes, cauldron state, wizard state, or charm-order bytes are touched.

The first Phase B marker patterns are:

- cauldron room:
  - colour 1;
  - ordinary block type 0x00;
  - positions:
    - (2, 2, 0);
    - (5, 2, 0);
    - (2, 5, 0);
    - (5, 5, 0);
- charm room:
  - colour 5;
  - spike block type 0x05;
  - the same four positions.

Those positions deliberately leave the central entry lanes clear:

- x = 3..4;
- y = 3..4.

This is stricter than the general non-start-room clearance rule, but only for
the marker pattern. It gives the player room to enter from any cardinal side
without being immediately blocked by the visual marker.

------------------------------------------------
Diagnostics

The Stage 8 diagnostics table was retitled to:

- Stage 8.2B - Safe Static Quest Room Dressing

The table now reports:

- quest role;
- required charm;
- anchor coordinates;
- reachability status;
- dressing phase;
- forced square status;
- marker colour;
- marker block type and count.

A healthy cauldron row reports a dressing summary like:

    8.2B-static-safe-dressing; open calm readable room; hazard low;
    safe static colour and block-run marker;
    location header and foreground block bytes only;
    forced square; colour 1; blocks 0x00 block x4

A healthy charm row reports:

    8.2B-static-safe-dressing; slightly guarded or puzzle-like room;
    hazard moderate; safe static colour and block-run marker;
    location header and foreground block bytes only;
    forced square; colour 5; blocks 0x05 spike block x4

The cache/diagnostic marker is:

- stage8-quest-dressing-20260711-2

------------------------------------------------
Verification

Automated checks:

- JavaScript syntax checks passed for:
  - SRC/runtime/knightlore-mapgen.js;
  - SRC/runtime/knightlore.js;
- git diff whitespace checks passed;
- webpack rebuild passed with only the existing bundle-size warnings;
- direct generator probing confirmed:
  - cauldron selector 0, colour 1, block type 0x00 x4;
  - charm selector 0, colour 5, block type 0x05 x4;
- browser smoke test confirmed:
  - the Stage 8.2B diagnostics table rendered;
  - the Dressing column was present;
  - no page errors were captured.

Manual testing:

- several sectors were traversed by hand;
- complete cauldron -> charm -> cauldron loops worked;
- dressed anchor rooms looked consistent on revisit;
- no reset, freeze, invalid room decode, or entry-side trapping was observed.

One recorded example:

    Current (6, -8) -> sector (0, -1).
    Required charm 12 (#12).
    Cauldron (6, -8) l(6, 0).
    Charm (0, -5) l(0, 3).
    Reachability: OK; anchors CH; path 9; searched 42; margin 2 chunk(s).

This is enough to mark Phase B complete as a practical play-test proof. It is
not an exhaustive proof over the infinite map, but the dressing rule is
coordinate-independent and uses the same compiler path for every sector.

------------------------------------------------
Files Updated

- SRC/runtime/knightlore-mapgen.js
  - questDressing metadata;
  - Phase B static marker definitions;
  - square/colour/block override for quest anchors.

- SRC/runtime/knightlore.js
  - questDressing propagation;
  - Stage 8.2B diagnostics formatting;
  - dressing summary in room summaries.

- SRC/static/index.html
  - Stage 8.2B title;
  - Dressing column;
  - updated cache marker.

- Docs/Plan 2026 07 08.txt
  - Phase A and Phase B details;
  - recommended questDressing payload shape.

- Docs/map-generation-algorithm.txt
  - Phase 8.2A/B algorithm notes;
  - marker patterns and safety boundary.

- Docs/info.txt
  - low-level static-room marker rule and memory-safety reminder.

------------------------------------------------
Current project state at end of 2026 07 13

- Stage 8.2 Phase A is complete.
- Stage 8.2 Phase B is complete.
- Quest cauldron/charm rooms are now identifiable by diagnostics and by safe
  in-game static room-entry dressing.
- The original renderer, game source, item slots, object records, cauldron
  state, wizard state, inventory state, and charm-order memory remain untouched.
- The next risky boundary is Phase C: probing whether actual original
  cauldron/charm objects can be represented safely, or whether the first
  playable quest loop should remain JS-side.


+============================================================+
| 2026 07 13 - Stage 8.2C Cauldron Visual Probe              |
+============================================================+

2026 07 13

Stage 8.2C has started, but only as a deliberately narrow original-cauldron
visual probe. It is not yet full original charm/cauldron gameplay injection.

The goal of this step was to answer the safest first Phase C question:

- can generated quest cauldron rooms display the original cauldron body and
  stew without touching original item slots, inventory, charm-order memory,
  wizard state, or cauldron-completion state?

------------------------------------------------
Memory Probe Results

Read-only snapshot probes recovered the original relevant bytes:

- background offset table at 0x6D06:
  - 0x12 -> 0x6FAE;
  - 0x13 -> 0x6FBF;
- wizard background 0x12 at 0x6FAE..0x6FBE:

    0x9E 0x98 0x68 0x80 0x05 0x05 0x18 0x10
    0x90 0xA0 0x60 0x80 0x05 0x05 0x00 0x12 0x00

- cauldron background 0x13 at 0x6FBF..0x6FCF:

    0x8D 0x80 0x80 0x80 0x0A 0x0A 0x18 0x10
    0x8E 0x80 0x88 0x80 0x00 0x00 0x00 0x12 0x00

- cauldron bubble template at 0xB8C8..0xB8D9:

    0xA0 0x80 0x80 0x80 0x05 0x05 0x0C 0x10
    0xB4 0x00 0x00 0x00 0x00 0xA0 0x00 0x00 0x00 0x00

The cauldron visual is therefore background id 0x13, formed by:

- 0x8D: cauldron body;
- 0x8E: cauldron contents / stew.

The bubbles are separate and remain risky because they may be connected to
wolf-form attack behaviour and live slots around 0x5C68.

------------------------------------------------
Runtime Changes

Phase C is opt-in:

- ?stage8c=1
- ?stage82c=1

When enabled, generated quest cauldron anchor rooms:

- are forced to selector 0;
- use a calm room colour;
- suppress the Phase B four-corner block marker;
- append background id 0x13;
- keep the normal generated wall/arch compiler path for exits.

The runtime restores the original cauldron background bytes and the original
0x12/0x13 background offset table entries when the visual probe is enabled.

The wizard background remains zeroed. The bubble template remains zeroed unless
the separate high-risk flag is used:

- ?stage8cbubbles=1

Normal Phase C visual testing should not use the bubble flag yet.

------------------------------------------------
Background ID Adjustment

Stage 7 custom rectangular tree-wall backgrounds had previously used 0x12 and
0x13 as convenient runtime ids. Phase C needs those ids restored to their
original meanings:

- 0x12: original wizard background;
- 0x13: original cauldron background.

The custom tree-wall ids were therefore moved to:

- 0x16: selector 2, 64x32 tree wall;
- 0x17: selector 1, 32x64 tree wall.

In the original background table, 0x16 and 0x17 are high-arch base entries.
The current generated map does not rely on those high-arch-base backgrounds,
so this is an acceptable runtime tradeoff for now. Authored maps should not
depend on original 0x16/0x17 high-arch-base visuals while the custom
rectangular tree-wall patch is active.

This keeps rectangular wooden rooms available while freeing 0x13 for the
original cauldron visual probe.

------------------------------------------------
Diagnostics And Verification

The diagnostics/cache marker is now:

- stage8-origin-088-blocks-20260713-1

The Stage 8 table title now covers both safe static dressing and the cauldron
visual probe:

- Stage 8.2B/C - Quest Room Dressing

With ?stage8c=1, a cauldron row should report:

- phase: 8.2C-cauldron-visual-probe;
- extra background: 0x13 cauldron;
- original cauldron visual;
- bubbles disabled.

Automated checks passed:

- JavaScript syntax checks for SRC/runtime/knightlore-mapgen.js;
- JavaScript syntax checks for SRC/runtime/knightlore.js;
- git diff whitespace check;
- webpack rebuild;
- direct generator probe for default Phase B and opt-in Phase C modes;
- browser smoke test with ?stage7sliding=1&stage8c=1.

The browser smoke test confirmed:

- the Stage 8.2B/C diagnostics table rendered;
- Phase C status text appeared;
- cauldron background 0x13 was restored;
- cauldron bubbles remained zeroed;
- no page errors were captured.

------------------------------------------------
Current project state at end of 2026 07 13

- Stage 8.2 Phase A is complete.
- Stage 8.2 Phase B is complete.
- Stage 8.2 Phase C has started as C1: original cauldron visual background
  only.
- The original cauldron body and stew can now be enabled in generated quest
  cauldron rooms with ?stage8c=1.
- The original bubbles are still disabled by default and should be probed
  separately.
- Collectable charm objects, item slots, inventory, charm-order memory,
  wizard state, and original cauldron-completion state remain untouched.

------------------------------------------------
Manual follow-up on 2026 07 13

The cauldron visual probe was manually tested across several sectors,
including a wooden-sector cauldron room. Cauldrons appeared at the expected
quest cauldron anchors.

The bubble flag was also tested:

    ?stage7sliding=1&stage8c=1&stage8cbubbles=1

Result:

- bubbles appeared in every room, not only cauldron rooms;
- bubbles retained the original wolf-killing behaviour;
- after death, respawn advanced through neighbouring logical rooms in a
  sequence such as:
  - (1,0) -> (2,0) -> ...
  - (0,-1) -> (0,-2) -> ...
- when the player died at (0,0), respawn stayed at (0,0).

This confirms that the bubble template is global/live-slot behaviour, not a
safe room-local cauldron dressing mechanism. The default remains to keep
bubbles disabled.

The built-in logical origin `(0,0)` was also updated to preserve the original
room 0x88 eight-block floor frame:

- block type 0x00;
- positions:
  - (2,6,0);
  - (1,5,0);
  - (5,6,0);
  - (6,5,0);
  - (6,2,0);
  - (5,1,0);
  - (1,2,0);
  - (2,1,0).

The central spawn/walk lanes remain clear.

------------------------------------------------
Stage 8.2C2 bubble-template probe, 2026 07 13

The first attempted C2 test used:

    ?stage7sliding=1&stage8c=1&stage8cbubbles=1&stage8cbubbles=cauldron

At that point the runtime read the first repeated stage8cbubbles value, so it
behaved as the global high-risk mode and produced bubbles in every room. This
confirmed that repeated URL parameters are too easy to misread during manual
experiments.

The parser was updated so repeated stage8cbubbles values use the last value
and report the full request chain in diagnostics, for example:

    requested "1 -> cauldron"

The diagnostics/cache marker for this C2 probe is now:

- stage8-c2-bubble-probe-20260713-2

C2 modes:

- no stage8cbubbles, or stage8cbubbles=off:
  keep the bubble template zeroed.
- stage8cbubbles=1 or stage8cbubbles=global:
  restore the template globally. This intentionally reproduces the dangerous
  "bubbles in every room" control.
- stage8cbubbles=cauldron:
  restore the template only while the logical center room has questRole
  cauldron; zero it elsewhere.

The C2 diagnostics row reports:

- interpreted mode;
- requested parameter chain;
- desired active/off state;
- template restored/zeroed/mixed readback;
- current logical room and quest role;
- number of template gate writes;
- first bytes read from live slot area 0x5C68.

This still writes only the static bubble template at 0xB8C8..0xB8D9. It does
not clear live bubble/object slots yet. If bubbles remain after leaving a
cauldron room, the next probe should target live-slot cleanup around 0x5C68.
If bubbles do not appear in a cauldron room, the template write is happening
too late and a pre-load hook will be needed.

------------------------------------------------
Manual validation on 2026 07 17

The C2 cauldron-gated bubble-template probe was manually tested with:

    ?stage7sliding=1&stage8c=1&stage8cbubbles=cauldron

Result: success.

- Bubbles appeared only in the logical quest cauldron room at (4,4).
- Bubbles did not appear in ordinary rooms, including the authored origin
  (0,0).
- The Stage 1 row `Stage 8.2C2 bubble probe` reported the expected state:
  desired off / template zeroed outside the cauldron room, and desired active /
  template restored in the cauldron room.
- Bubbles were harmless when the player entered in human form.
- Bubbles retained the original wolf-killing behaviour when the player entered
  in wolf form.

This confirms that the C2 template gate is early enough and specific enough
for room-local cauldron bubbles under the current sliding-cross model.

Observed caveats:

- Bubble animation is too fast.
- The requested-charm flashing is also too fast to appreciate comfortably.
- After a bubble death, the player respawns in the room opposite the side used
  to enter the cauldron. Example: if the player entered the cauldron room
  through the east door, death respawns the player at the east door of the room
  west of the cauldron.

The animation speed is noted but not currently part of the objective. The
respawn displacement is odd enough to deserve its own later probe. It is likely
related to the original engine's death/entry-side/transition state rather than
to the static bubble-template gate itself.

Current C2 conclusion:

- Stage 8.2C2 is successful as a room-gated bubble activation proof.
- Do not treat the respawn displacement as blocking C2.
- Next related probe, when needed: inspect and/or patch the death respawn
  transition state so bubble deaths return the player to the intended cauldron
  room/entry side under the virtual map model.


+============================================================+
| 2026 07 18 - Stage 8.4 C3.0 Read-Only Anatomy              |
+============================================================+

2026 07 18

Stage 8.4 / C3.0 has started as a strictly read-only original
charm/item/cauldron mechanic probe.

The current diagnostics build marker is:

- stage84-c30c-cauldron-gate-20260718-1

A new table was added below the Stage 8 quest diagnostics:

- Stage 8.4 C3.0 - Read-Only Mechanic Anatomy

The table samples and decodes:

- current logical room, quest role, and required charm context;
- 0x5C48..0x5C67, first live item/charm candidate slot;
- 0x5C68..0x5C87, second live item/charm/bubble/display candidate slot;
- the first eight expanded dynamic records at 0x5C88 + n * 0x20;
- all 32 original 9-byte static object records at 0x6FF2..0x7111.

This pass does not write original item slots, inventory bytes, charm-order
bytes, cauldron-completion bytes, or static item-table records. It is intended
to let manual play reveal which bytes change when entering cauldron/charm
rooms and, in later probes, when picking up, carrying, dropping, or leaving
items.

What to watch during manual testing:

- Whether 0x5C48 or 0x5C68 changes when entering the generated charm room.
- Whether 0x5C68 changes when entering the cauldron room with original visual
  and room-gated bubbles enabled.
- Whether any 0x6FF2 static object records change after leaving a room where
  an item has moved or been dropped.
- Whether dynamic records around 0x5C88 expose stable item/display/bubble
  sprite records that can later be targeted safely.

Current C3.0 conclusion:

- Probe infrastructure is in place.
- Gameplay mechanics remain untouched.
- The next useful step is manual observation in origin, cauldron, and charm
  rooms before attempting C3.1 item injection.

Manual observation update:

- At logical origin (0, 0), both 0x5C48..0x5C67 and 0x5C68..0x5C87 were all
  zero.
- In the generated cauldron room (4, 4), 0x5C68..0x5C87 became active with
  sprite 0xA0 and byte +8 equal to 0xB4, while the current physical room was
  still 0x88.
- In the generated charm metadata room (3, 0), both live slots remained zero,
  which is expected because actual charm/item injection has not started.
- The C3.0 table was corrected so byte +8 is only labelled as a room for
  item/charm-like live objects. For cauldron bubble/display objects it is now
  reported as a state/sprite-like byte.
- A direct read of the Knight Lore .z80 snapshot confirmed 32 raw non-zero
  records in 0x6FF2..0x7111. This is a raw memory-table count and may differ
  from a later gameplay classification such as visible, active, duplicate, or
  special collectable objects.
- A transient cauldron was observed once at logical origin (0, 0), blocking the
  player; a reload cleared it. The likely cause is a startup ordering race:
  Stage 8C restores background id 0x13 globally, while the original
  unvirtualized room 0x88 also contains 0x13. If the Stage 7 cross is not
  present before the first room decode, the original cauldron can leak into the
  origin.
- Startup was hardened so JSSpeccy loads the snapshot paused, waits for the
  emulator ready callback, runs a startup-prime memory sample/injection, and
  only then starts execution. During Stage 7 sliding, the physical cross is
  written before the 0x13 cauldron background is restored.
- Live-slot diagnostics now list non-zero byte offsets across the full sampled
  32-byte slot, because the first 16 bytes shown in the byte preview can be
  zero while later bytes still make a slot non-empty.
- Follow-up correction: the first startup-prime implementation used the
  existing JSSpeccy onReady hook, but that hook fired before openUrlList had
  necessarily finished loading the Knight Lore .z80 snapshot. That meant the
  startup-prime writes could be overwritten by the snapshot load, producing a
  black first room or the original Knight Lore map after restart. JSSpeccy
  readiness now waits for the requested URL/snapshot load to complete. If the
  Stage 7 cross is not successfully injected, the runtime keeps execution
  paused instead of auto-starting into the original map.
- C3.0b expands the read-only table with carried/cauldron-state anatomy:
  - 0x5BB3 pickup_drop_pressed;
  - 0x5BB4 objects_carried_changed;
  - 0x5BBB objects_put_in_cauldron;
  - 0x5BC3 all_objs_in_cauldron;
  - 0x5BC4 obj_dropping_into_cauldron;
  - 0x5BD8..0x5BE7 inventory handoff and the three carried-object display
    slots;
  - live-slot +16/+17 pointers back to the original static object table.
- The table also reports whether the original hard-coded cauldron room check
  would be true for the current physical center. Under Stage 7 recentering,
  physical room 0x88 is normally true even when the logical room is not a
  quest cauldron, so C3.1 must add a gate/patch before real original item drops
  are allowed.
- C3.0c adds that gate as a narrow runtime patch. The original pickup/drop
  routine has two hard-coded CP 0x88 instructions relevant to cauldron drops:
  - 0xC086 FE 88, compare immediate at 0xC087;
  - 0xC0C9 FE 88, compare immediate at 0xC0CA.
- The runtime verifies the opcode byte is still 0xFE and writes only the
  immediate byte. In logical quest cauldron rooms the immediates are 0x88. In
  all other logical rooms they are 0xFF, so the original physical-room check
  cannot accidentally treat every sliding center room as a cauldron.
- No actual charms, inventory bytes, charm-order bytes, completion bytes, or
  static object records are injected by C3.0c. This is only the acceptance gate
  needed before C3.1 can safely try a disposable charm.
- C3.1 has now started as an opt-in disposable charm probe.
- The diagnostics build marker is now:
  - stage84-c31-disposable-charm-20260718-1
- C3.1 is enabled with ?stage84c31=1 or ?stage8c31=1, in addition to the
  usual Stage 7 / Stage 8 cauldron-visual flags used for this line of tests.
- The probe rewrites exactly one original static object-table record:
  - record 31 at 0x7109..0x7111.
- When a logical quest charm anchor is present in the current five-room
  physical cross, that record is armed with:
  - sprite 0x60 + (requiredCharm.id % 7), keeping the value in 0x60..0x66;
  - X/Y/Z 0x80/0x80/0x80;
  - current and start room equal to the target physical room id.
- When no logical charm anchor is present in the current physical cross, the
  record is disabled with current/start room 0xFF.
- If the carried/inventory queue at 0x5BD8..0x5BE7 points back to 0x7109, the
  runtime skips static-record reseeding so a carried disposable probe item is
  not overwritten during sliding recentering.
- C3.1 still does not directly write inventory bytes, charm-order bytes,
  completion bytes, or JS quest-completion state. It is only a controlled
  original-static-record placement probe.
- Testing caveat: during this disposable proof, if the item is dropped in a
  non-cauldron room, pick it back up before leaving the room. C3.2 is where we
  will decide how to preserve or virtualize dropped/carried item state cleanly.
- C3.2 has now started as a carried/inventory state probe.
- The diagnostics build marker is now:
  - stage84-c32-carried-inventory-20260718-1
- The Stage 8.4 table now includes three C3.2 rows:
  - C3.2 carried identity;
  - C3.2 carry stability;
  - C3.2 current-sector match.
- The original carried queue is decoded as four 4-byte slots:
  - 0x5BD8..0x5BDB inventory handoff;
  - 0x5BDC..0x5BDF carried display 0;
  - 0x5BE0..0x5BE3 carried display 1;
  - 0x5BE4..0x5BE7 carried display 2.
- C3.2 treats a carried disposable charm as stable when the carried slot keeps
  a 0x60..0x66 sprite and a pointer back to 0x7109 -> static object 31 while
  the player walks through Stage 7 recenter transitions.
- Correction from manual review: 0x60..0x66 are the seven actual quest charm
  types. The original 14-object requirement asks for each type twice; it is not
  a set of 14 distinct charm identities. Object 0x67 is extra life/consumable,
  not a quest charm.
- The C3.1 row now says "last seeded target" to avoid implying that the saved
  source room is the current logical room after the player has carried the
  item away.
- The original cauldron requested-object display is still intentionally
  independent. C3.2 compares JS quest metadata with carried identity, but C3.3
  is still the first stage where requested-object display or cauldron
  acceptance should be patched or bypassed.


+============================================================+
| 2026 07 20 - Stage 8.4 C3.3/C3.4 Hybrid Cauldron Proof     |
+============================================================+

2026 07 20

Stage 8.4 has now proved the first controlled hybrid version of the dispersed
quest mechanic:

- the player can collect an original-style quest charm from a generated logical
  charm room;
- the original carried-item queue survives Stage 7 sliding/recentering;
- the player can carry the charm to a generated logical cauldron room;
- the player can perform the normal human-form drop ritual at the cauldron;
- JS persistent sector state can mark the sector as completed;
- with C3.4 enabled, the original Knight Lore cauldron logic can also accept
  the same drop and produce original feedback.

The relevant opt-in flags for this proof were:

- ?stage7sliding=1
- ?stage8c=1
- ?stage84c31=1
- ?stage84c33=1
- ?stage84c34=1

Optional room-gated bubbles remain:

- ?stage8cbubbles=cauldron

C3.3 status:

- C3.3 is the drop-gated JS-side cauldron acceptance probe.
- A carried quest charm is accepted only when:
  - the current logical room is a quest cauldron;
  - the carried sprite is one of the seven real charm types, 0x60..0x66;
  - the carried sprite matches the current sector's required charm type;
  - the player performs a drop action in human form.
- The charm source sector is diagnostic only. A charm is a charm; if another
  sector asks for the same charm type, the player should be allowed to use the
  carried charm there.
- Before C3.4, C3.3 could complete the JS sector state but the original
  cauldron display could still request a different object, making the visual
  ritual confusing.

C3.4 implementation:

- C3.4 is the original cauldron request-order patch.
- It is enabled with ?stage84c34=1 or ?stage8c34=1.
- It uses the original request mechanism identified in the disassembly:
  - 0x5BBB is objects_put_in_cauldron, the original current request index;
  - 0xC27D..0xC28A is objects_required, the mutable 14-byte request table;
  - ret_next_obj_required returns objects_required[objects_put_in_cauldron].
- The runtime patches only the active request byte:
  - address = 0xC27D + objects_put_in_cauldron;
  - value = current JS sector required charm index, 0..6.
- The patch is applied in the current logical cauldron room, and also pre-armed
  from a directly reachable adjacent room. The adjacent pre-arm matters because
  the original cauldron display may be initialized during room load.

Manual proof in sector (0,0):

- The sector required charm 11, mapped to original charm index 3 and sprite
  0x63, chalice/vase.
- In the room adjacent to the cauldron, logical (4,3), the C3.4 row reported:
  - target (4,4), pre-armed adjacent north cauldron;
  - objects_put 0x00;
  - active request slot 0 at 0xC27D;
  - desired 3 -> 0x63 chalice/vase;
  - previous 3 -> 0x63 chalice/vase.
- In the cauldron room, logical (4,4), the C3.4 row reported:
  - current logical cauldron;
  - the same active slot and desired chalice/vase request.
- After the player dropped the chalice/vase in human form:
  - the original game made its normal successful cauldron colour shift;
  - objects_put_in_cauldron advanced from 0x00 to 0x01;
  - the carried queue at 0x5BD8..0x5BE7 became empty;
  - disposable static object record 31 was observed as 0x00@0x88;
  - the JS sector state became completed.

Conclusion from this proof:

- The original cauldron acceptance path is genuinely participating when C3.4 is
  enabled. This is not merely an external JS completion marker.
- The original item was consumed/removed by original mechanics during the
  successful drop.
- The original colour-shift feedback is usable evidence that the drop was
  accepted by the original engine.

Completed-sector guard:

- The first successful C3.4 test exposed a new risk: after the original counter
  advanced to 0x01, the runtime would otherwise patch request slot 1 while the
  player was still standing in the already completed local cauldron.
- A guard was added in build:
  - stage84-c34-completed-guard-20260720-1
- If the target JS quest sector is already completed, C3.4 now reports:
  - target completed yes;
  - target sector already completed;
  - request slot left unchanged.
- Manual retest confirmed the guarded result at logical (4,4):
  - objects_put 0x01;
  - slot 1 / patch address 0xC27E;
  - target completed yes;
  - last action current logical cauldron: target sector already completed;
    request slot left unchanged.
- The C3.4 `writes` count is cumulative for the session. It may remain non-zero
  after a previous write, but the decisive completed-sector result is the last
  action saying that the request slot was left unchanged.

Remaining risk:

- C3.4 now prevents one completed local cauldron from repeatedly advancing the
  original 14-object counter.
- It does not yet solve the broader policy question of what should happen to
  the original global counter across many different dispersed sectors.
- Under the current proof build, each newly completed sector may still advance
  objects_put_in_cauldron once. Before this becomes normal gameplay, we need a
  deliberate counter policy: keep the original counter as a global progress
  signal, virtualize it per sector, reset/isolate it after each sector, or use
  only JS-side quest progression while preserving selected original feedback.

Current C3.4 conclusion:

- Stage 8.4 C3.4 is successful as a controlled hybrid proof.
- We can combine:
  - JS deterministic sector metadata and persistent sector completion state;
  - original item pickup/carry/drop;
  - original cauldron request display;
  - original cauldron acceptance and colour-shift feedback.
- The next design decision is not whether the hybrid is possible. It is how
  much of the original global 14-object cauldron counter should be kept,
  virtualized, or isolated for the dispersed infinite-map design.


+============================================================+
| 2026 07 21 - Stage 8.4 C3.5 Cauldron Timing Probe          |
+============================================================+

2026 07 21

Stage 8.4 C3.5 investigated the two visual/timing issues exposed by the real
cauldron probe:

- the cauldron bubble cycle was much faster than was comfortable to read;
- the requested charm flash was too brief, even after the original request
  table was patched correctly by C3.4.

C3.5a:

- C3.5a remained read-only.
- It measured the live cauldron bubble/display slot at 0x5C68..0x5C87.
- The row classified:
  - 0xA0..0xA7 as bubble / wolf-attack bubble frames;
  - 0xA8..0xAE as requested-object display flashes.
- Manual observations confirmed very short frame gaps for both the bubble
  frames and the requested-object flashes.

C3.5b:

- C3.5b tested post-frame live-slot holding.
- The first predecessor-replay attempt was rejected because wolf-form bubbles
  stopped killing the player.
- Request-only holding preserved wolf danger but did not slow the bubbles.
- Human-form bubble holding, including the later sprite/Z/dZ hold, still did
  not produce a clean enough visual rhythm.
- Conclusion: post-frame pokes into 0x5C68 are useful for diagnosis, but are
  not the preferred final timing lever.

C3.5c:

- C3.5c moved the experiment to the original bubble/update routine.
- It is enabled with:
  - ?stage84c35c=1
  - ?stage84c35cperiod=4 by default, clamped to 2..12.
- C3.5b is suspended automatically while C3.5c is enabled.
- Confirmed routine targets:
  - 0xB8E8: CALL dec_dZ_and_update_XYZ inside upd_160_to_163;
  - 0xB8EB: CALL next_graphic_no_mod_4 inside upd_160_to_163;
  - 0xB916: the normal set_wipe_and_draw_flags exit;
  - 0xB926: LD (IX+0),0xA0 inside upd_168_to_175.

The first routine-level attempt patched the two calls to NOP. This proved that
the right code path had been found, but the human-form bubble rhythm was
irregular.

The working revision is:

- on throttled human-form bubble frames, patch 0xB8E8 to JP 0xB916;
- keep 0xB8EB restored/original;
- when the live sprite is a request display, 0xA8..0xAE, patch 0xB926 to
  NOP NOP NOP NOP on throttled frames;
- restore all original bytes on active frames, outside logical cauldron rooms,
  and whenever the player is not human.

Manual validation:

- In human form, the bubble rhythm is now readable. With period 4 it is a bit
  slow, but acceptable for a first proof.
- The requested charm display is now much easier to see.
- In wolf form, the routine bytes are restored and the bubbles still kill the
  player.
- The C3.5c diagnostics row reports current/original bytes for the motion call,
  graphic call, and request-reset instruction, which gives us a quick safety
  check before and after testing.

Recommended run for the next manual pass:

- Use the C3.4 hybrid stack plus C3.5c:
  - ?stage7sliding=1
  - ?stage8c=1
  - ?stage8cbubbles=cauldron
  - ?stage84c31=1
  - ?stage84c33=1
  - ?stage84c34=1
  - ?stage84c35c=1
- Start with ?stage84c35cperiod=4.
- If the timing feels too slow over a two- or three-sector run, try
  ?stage84c35cperiod=3.

Expected anomalies to watch during the 2026 07 22 run:

- Bubble-death respawn displacement is still unresolved. A bubble death may
  respawn the player in a neighbouring room related to the entry side.
- The original objects_put_in_cauldron counter is still global. C3.4 patches
  the active request slot for the current logical cauldron, but multi-sector
  policy for that global counter is not final.
- Carrying a charm across sector boundaries should work, but should be watched
  carefully because C3.2 only proved stability over short routes.
- Revisited rooms should remain deterministic. If a room changes unexpectedly,
  check whether it is a real generator issue or a completed-sector dressing
  state change.
- The C3.5c diagnostics row should show original bytes outside cauldron rooms
  and in wolf form. If it does not, reload the snapshot before continuing.

Current C3.5 conclusion:

- C3.5c is the best timing probe so far.
- It preserves wolf danger, improves request readability, and keeps all changes
  opt-in runtime patches.
- It is ready for a two- or three-sector continuity run, but should not yet be
  treated as final gameplay policy.


+============================================================+
| 2026 07 21 - Stage 8.4 C3.5e Bubble-Death Respawn Guard    |
+============================================================+

2026 07 21

A three-sector manual run exposed the C3.5e respawn issue as a real gameplay
blocker:

- the player entered a logical cauldron room as a wolf;
- the cauldron room did not have an exit on the side opposite the entry side;
- the bubbles killed the wolf;
- the original death/respawn routine sent the player to the physical neighbour
  on that opposite side;
- that neighbour represented a missing logical room, so the emulator rendered a
  black empty room and the player could not recover.

First mitigation:

- Build marker:
  - stage84-c35e-bubble-respawn-20260721-1
- A new C3.5e guard row was added to the Stage 8.4 diagnostics table.
- The guard is enabled by default with Stage 7 sliding.
- It can be disabled with:
  - ?stage84c35e=0
- While in a logical cauldron room, the guard records:
  - entry side;
  - logical coordinate;
  - physical room id;
  - player body XYZ;
  - player head XYZ;
  - current lives.
- If the previous sample was a wolf-form logical cauldron with bubbles active,
  and the current sample shows a life drop, the guard arms a pending
  bubble-death correction.
- If the original death routine then changes the active physical room away from
  0x88, the guard writes the active room-id slots back to 0x88 and restores the
  body/head XYZ triplets to the recorded cauldron entry coordinates.

What to test next:

- Reproduce the original failure: enter a cauldron room as wolf from a side
  where the opposite-side neighbour does not exist.
- After bubble death, the player should remain in the same logical cauldron
  room, at the same recorded entry side.
- The Stage 7 row should not report a normal recenter into the missing
  neighbour.
- The C3.5e row should increment `corrections` and show the intercepted
  physical room.

Known risk:

- This is a post-detection correction. If the original engine has already
  decoded the blank placeholder room before the correction takes effect, the
  logical room id may be fixed but the visible room may still be wrong.
- If that happens, the next mitigation is to pre-arm the expected
  death-respawn physical neighbour with duplicate cauldron room data during the
  death sequence.


+============================================================+
| 2026 07 22 - C3.5e Trigger Revision                        |
+============================================================+

2026 07 22

Manual testing in sector (0,0) showed that the first C3.5e guard armed
correctly, but did not correct the displacement:

- before death, the row showed player wolf, lives 0x02, logical cauldron (4,4),
  entry south, pending none, corrections 0;
- after the visible respawn/displacement case, the lives byte could still read
  0x02;
- therefore `0x5BBA` is not reliable enough as the only visible-death trigger.

Revision:

- Build marker:
  - stage84-c35e-form-jump-20260722-1
- C3.5e now also watches for:
  - previous sample armed as wolf in a logical cauldron with active bubbles;
  - current sample no longer wolf;
  - either a physical-neighbour room load or a large body XYZ jump.
- The correction now runs even if 0x5C10 is still 0x88, so it can restore the
  player to the recorded cauldron entry point in the same physical room.
- The diagnostics row now includes:
  - current body XYZ;
  - trigger kind;
  - measured XYZ jump.

What to test:

- Repeat the sector (0,0) cauldron wolf-death test.
- The row should increment `corrections`.
- The `trigger` field should read either `wolf respawn body jump` or
  `wolf respawn physical-room change`.
- The player should return to the recorded entry side instead of the opposite
  side.

Follow-up trace:

- A later run clarified that the player appeared at the correct entrance, but
  in the room one step opposite the previous room with respect to the cauldron.
- The row after death showed:
  - lives 0x03;
  - current logical coordinate (4,5);
  - previous cauldron entry (4,4) south;
  - corrections 0.
- This means Stage 7 had already accepted the death respawn as a normal
  transition before C3.5e acted.

Post-recenter revision:

- Build marker:
  - stage84-c35e-post-recenter-20260722-1
- C3.5e now runs a second correction pass immediately after normal Stage 7
  recentering.
- If that pass sees a life drop from a previously armed wolf-form cauldron, it
  rolls the logical center and static cross back to the recorded cauldron room,
  then restores room-id bytes and body/head XYZ.
- This should stop the persistent one-room logical shift.
- If the visible room still remains the already-expanded neighbour, that tells
  us the correction is still too late for working-memory room contents and the
  next fix must pre-arm the expected death target with duplicate cauldron data.

Entry-lifedrop refinement:

- Build marker:
  - stage84-c35e-entry-lifedrop-20260722-1
- The newest trace showed the remembered entry `(4,4)` still present, lives
  lower at `(4,5)`, but corrections still zero. That means the immediate
  previous-sample detector had already lost the cauldron as its source.
- C3.5e now treats the recorded cauldron entry itself as a short-lived death
  source.
- If the player entered the cauldron from the south, the expected bad respawn is
  the room north of the cauldron. The guard now checks that geometry and the
  lower lives byte.
- Expected trigger text:
  - entry life drop after recenter

Latched-entry refinement:

- Build marker:
  - stage84-c35e-latched-entry-20260722-1
- The next manual trace showed a narrower race:
  - before death, the real entry was `(4,4)` south with lives `0x04`;
  - after death, the logical center again read `(4,4)` and lives read `0x03`;
  - however, the engine had produced a false north-entry transition, replacing
    the south-entry snapshot before the guard used it.
- C3.5e now maintains a separate armed-entry latch. It preserves the real
  cauldron entry side, entry XYZ, and pre-death lives independently of later
  transition diagnostics.
- After correction, the latch is re-baselined at the restored entrance and new
  life count so a repeated death during the same visit remains protected.
- The C3.5e row now shows `armed entry ... lives ... age ...`.
- Expected result after a bubble death:
  - trigger `latched cauldron life drop`;
  - `corrections` increments;
  - the player remains in logical cauldron `(4,4)` at the original south
    entrance.


+============================================================+
| 2026 07 22 - C3.5e Bubble-Death Guard Confirmed            |
+============================================================+

2026 07 22

The latched-entry C3.5e guard passed its manual wolf-death test in the sector
(0,0) cauldron.

Immediately before death, diagnostics reported:

- logical room (4,4), role cauldron;
- current entry south at X=0x81, Y=0xFF, Z=0x80;
- armed entry (4,4) south with lives 0x04 and age 0;
- corrections 0 and writes 0.

Immediately after death, diagnostics reported:

- lives reduced from 0x04 to 0x03;
- trigger `latched cauldron life drop`;
- corrections 1 and writes 4;
- attempted wrong physical room 0x98;
- room-id writes to 0x5C10 and 0x5C30;
- body/head XYZ restoration through 0x5C09..0x5C0B and
  0x5C29..0x5C2B;
- logical center still (4,4), role cauldron;
- real and armed entry still south;
- armed latch re-baselined to lives 0x03.

The player remained in the same logical cauldron room at the entrance used
before death. Body Y later reading 0x49 rather than the stored boundary value
0xFF is expected: the original engine had already moved the restored player
inward from the entrance.

Conclusion:

- The original bubble-death routine did attempt to load physical room 0x98.
- The JS guard caught the life loss, restored physical center 0x88, preserved
  logical center (4,4), restored the player coordinates, and safely re-armed.
- The feared missing-room/black-room lockout is fixed for the tested timing
  path.
- The duplicate-cauldron pre-arm fallback is not currently needed.
- Stage C3.5e is empirically complete.
