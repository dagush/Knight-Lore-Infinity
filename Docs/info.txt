Coords:
My student detected that the exact expressions to go from low-resolution integer coordinates, where objects are initialised in memory, are translated to high-resolution coordinates are:
    X_h = X_L * 16 + X_l * 8 + 72
    Y_h = Y_L * 16 + Y_l * 8 + 72
    Z_h = Z_L * 12 + Z_l * 4 + ScreenZ
Where X_L, Y_L and Z_L are the low resolution coordinates, the _l is information related to the sprites and allow to work with self-moving objects, and ScreenZ is always 128.

Main Character:
    The head location information is located at 5C20h, 5C2Ah and 5C2Bh for X, Y and Z.
    The body location information is at         5C09h, 5C0Ah and 5C0Bh.
    Sprites, as stored in 0x5C08 (body) and 0x5C28 (head):
        0x10 to 0x15: human body west (6 sprites)
        0x18 to 0x1D: human body east
        0x20 to 0x27: human head west (8 sprites)
        0x28 to 0x2F: human head east
        0x30 to 0x35: wolf body west (6 sprites)
        0x38 to 0x3F: wolf body east
        0x40 to 0x47: wolf head west (8 sprites)
        0x48 to 0x4F: wolf head east
        0x5C to 0x5F: transformation (4 sprites)
    Orientation Summary:
        Positions 5C41h and 5C45h indicate the head and body sprite codes.
        Positions 5C0Fh and 5C2Fh contain the mirror flags for orientation.
        0x5C41 == 0x5C45: 0x18 human west/north, 0x19 human east/south,
                          0x1D wolf west/north,  0x1E wolf east/south.
        0x5C0F >= 0x4C -> south/north, else: west/east
        0x5C2F == 0x7E wolf north
    Pseudocode for character orientation:
        sprite_body = contents of 0x5c41
        if sprite_body is 0x18 or 0x19: body is human, else: body is wolf
        if contents of 0x5c0f >= 0x4c:
            direction is either North or South
            if sprite_body is 0x18 or 0x1d:
                direction is North,
            else: direction is South
        else:
            direction is either west or east
            if sprite_body is 0x18 or 0x1d:
                direction is West,
            else: direction is east
    Renderer compass/view convention:
        View names describe the side where the camera is placed, not the direction it looks.
        North view means the camera is north of the room/character and looks inward, toward the center.
        Therefore, if the character is facing north and the camera side is north, the rendered billboard should show the front.
        If the character is facing north and the camera side is south, it should show the back.
        The same rule is used for east, west, and south.
        The camera side is computed from the room center to the camera, not from the moving character to the camera.
        This keeps the chosen billboard side stable while the character walks.
        Diagonal full-3D character billboard policy:
            Upper south east (game): east-facing billboard, live game sprite ids.
            Upper north west: west-facing billboard, opposite live sprite side, final texture mirror in X.
            Upper south west: south-facing billboard, relative sprite side.
            Upper north east: north-facing billboard, opposite relative sprite side, final texture mirror in X.
        Final X mirroring also includes the live character mirror flags:
            body uses 0x5C0F;
            head uses 0x5C2F;
            transformation sprites use either flag.
        Policy mirror and live mirror are combined as toggles, so two mirrors cancel.


ghost: 0x50 to 0x53 (4 sprites)

Current Room
     id: 5C10h
     color: 5BADh
     size: 5BABh, 5BACh and 5BAEh (X, Y, Z)
     Walls: use static information

Room/location shape and background presets:
     In the static location entry, byte +2 combines the room size selector with the
     low 3-bit attribute/colour value. The background ids follow this byte.
     If foreground block data exists, the background list is followed by 0xFF
     and then the block/count groups.
     If there is no foreground block data, do not write a 0xFF separator. Let
     the entry end after the background ids. Original room 0x77 follows this
     background-only pattern. Adding 0xFF with no block bytes after it can make
     retrieve_screen enter the foreground decoder and read the next room id as
     block data, which can crash or reset the game.
     The visual wall shape is not controlled by a separate hidden attribute: the
     size selector and the chosen wall/background preset ids must agree.
     This explains the Stage 3 test where 0x5BAB..0x5BAE correctly reported a
     narrow room, but the room looked square because it still used square/tree
     background ids.
     Confirmed size selectors:
        selector 0: 64, 64, 128
        selector 1: 32, 64, 128
        selector 2: 64, 32, 128
     Original static-location patterns:
        selector 0: square rooms. Common wall/background ids include 0x0C and
                    tree room combinations using 0x0F, 0x10, and 0x11.
        selector 1: rectangular 32x64 rooms. These use wall id 0x0E and only
                    north/south exits on the narrow ends.
        selector 2: rectangular 64x32 rooms. These use wall id 0x0D and only
                    east/west exits on the narrow ends.
     Practical rule for injected rooms:
        For selector 1, use north/south exits and wall id 0x0E.
        For selector 2, use east/west exits and wall id 0x0D.
        Avoid doors on the long sides of rectangular rooms.
     Wooden/tree caveat:
        The original data only uses the tree/wood backgrounds 0x0F, 0x10, and
        0x11 with square rooms. There do not appear to be stock rectangular
        tree/wood wall definitions, so rectangular wooden rooms would require
        custom background definitions later.
        Current experimental Stage 7 rectangular tree rooms install custom
        background definitions in the unused tail of the erased location table
        and use runtime background ids 0x16 and 0x17 for them:
           0x16: custom tree wall size 2, for selector 2 rooms.
           0x17: custom tree wall size 3, for selector 1 rooms.
        In the original background table, 0x16 and 0x17 are high-arch base
        entries. The current generated map does not depend on those high-arch
        base visuals, so the runtime can reuse their ids for the custom
        rectangular tree walls.
        They also use tree arch ids 0x04..0x07 for legal narrow-side exits.
        Their custom wall records keep only source records that already use
        Z/base byte 0x80. Raised stone-wall layers are discarded rather than
        moved down, because lowering them can cover otherwise valid arches.
        The current long-wall fill strategy copies the complete original
        square-tree wall pattern and translates only the room-size plane:
           selector 1 / custom 0x17:
              full west wall pattern, x moved from 0x3F to 0x5F.
              rows use y = 0x49, 0x58, 0x68, 0x78, 0x88, 0x98, 0xA8, 0xB8.
           selector 2 / custom 0x16:
              full north wall pattern, y moved from 0xC0 to 0xA0.
              rows use x = 0x48, 0x58, 0x68, 0x78, 0x88, 0x98, 0xA8, 0xB8.
        The runtime removes the matching stone-derived ground wall records
        before appending these translated tree records, to avoid duplicate
        overlapping panels.
        Earlier attempts to use only the two filler records left holes.
        Lowering raised stone layers could cover valid arches, and hand-filled
        new explicit records caused unstable player/emulator behaviour. These
        attempts were reverted.
        This is new Knight Lore-compatible data, not a pattern observed in the
        original room dump. IDs 0x12 and 0x13 are reserved again for their
        original meanings: wizard background and cauldron background.
     Fixed-size entry caveat:
        When testing by rewriting a room entry without changing its total byte
        length, preserve the number of background slots before 0xFF. Cleaned
        null expansion ids may be used as fillers only when their expansion
        data is confirmed zeroed in the current runtime mode. In particular,
        do not use 0x13 as a filler when Stage 8.2C cauldron visual probing is
        enabled, because 0x13 is restored to the original cauldron background.
     Stage 8.2B quest-room marker rule:
        Quest cauldron/charm anchor rooms are visually dressed without using
        original cauldron, wizard, item, inventory, or charm-order mechanics.
        The generator forces these anchor rooms to selector 0 for the first
        safe visual proof and changes only compact static location-entry bytes:
           header colour/attribute;
           foreground block runs.
        Cauldron marker:
           colour 1;
           block type 0x00 at (2,2,0), (5,2,0), (2,5,0), (5,5,0).
        Charm marker:
           colour 5;
           block type 0x05 at the same four positions.
        These positions leave x=3..4 and y=3..4 clear, so the central entry
        lanes remain open from every cardinal side. Do not replace this with
        original background ids 0x12/0x13 or item/object records until a later
        read-only memory probe proves the original mechanics are safe to reuse.
     Stage 8.2C cauldron visual probe:
        Phase C is currently only a cauldron-background visual probe, enabled
        explicitly with ?stage8c=1 or ?stage82c=1. It is not yet full original
        charm/cauldron gameplay injection.
        Read-only probes recovered the original expansion data:
           background offset table at 0x6D06:
              0x12 -> 0x6FAE, 0x13 -> 0x6FBF.
           wizard background 0x12 at 0x6FAE..0x6FBE:
              0x9E 0x98 0x68 0x80 0x05 0x05 0x18 0x10
              0x90 0xA0 0x60 0x80 0x05 0x05 0x00 0x12 0x00
           cauldron background 0x13 at 0x6FBF..0x6FCF:
              0x8D 0x80 0x80 0x80 0x0A 0x0A 0x18 0x10
              0x8E 0x80 0x88 0x80 0x00 0x00 0x00 0x12 0x00
           bubble template at 0xB8C8..0xB8D9:
              0xA0 0x80 0x80 0x80 0x05 0x05 0x0C 0x10
              0xB4 0x00 0x00 0x00 0x00 0xA0 0x00 0x00 0x00 0x00
        With ?stage8c=1, generated quest cauldron anchor rooms are forced to
        selector 0, use a calm colour, emit no Phase B corner blocks, and append
        background id 0x13. This displays the original cauldron body and stew
        while leaving original item slots, charm-order bytes, inventory bytes,
        wizard state, and cauldron-completion state untouched.
        The bubble template remains zeroed unless the separate experimental
        ?stage8cbubbles=1 flag is also set. That flag should be treated as
        high risk because bubbles may be coupled to wolf-form attack behaviour
        and original live slots around 0x5C68.
        Manual test result, 2026 07 13: restoring the bubble template with
        ?stage8cbubbles=1 made bubbles appear in every room, not only cauldron
        rooms. They retained the original wolf-killing behaviour. After death,
        respawn advanced through neighbouring logical rooms in a sequence such
        as (1,0), (2,0), ... or (0,-1), (0,-2), ..., while deaths at (0,0)
        respawned at (0,0). Treat bubbles as a global/live-slot behaviour until
        per-room activation is understood.
        Stage 8.2C2 URL modes:
           no stage8cbubbles parameter, or stage8cbubbles=off:
              keep the bubble template zeroed.
           stage8cbubbles=1 or stage8cbubbles=global:
              restore the bubble template globally. This reproduces the
              high-risk "bubbles in every room" behaviour and is useful only
              as a control experiment.
           stage8cbubbles=cauldron:
              restore 0xB8C8..0xB8D9 only while the current logical center
              room has questRole cauldron; zero the same template elsewhere.
              This is a timing probe, not a complete fix, because it does not
              clear or patch live bubble/object slots around 0x5C68.
        If multiple stage8cbubbles parameters are present, the runtime uses
        the last one and reports the chain in diagnostics, for example
        "1 -> cauldron".
        The Stage 8.2C2 diagnostics row reports requested mode, desired state,
        template readback, current logical role, gate writes, and live bytes at
        0x5C68.
        Manual test result, 2026 07 17: stage8cbubbles=cauldron behaved
        correctly. Bubbles appeared only in the logical quest cauldron room
        (sector (0,0), cauldron at (4,4)) and not in ordinary rooms. They were
        harmless to human Sabreman and retained the original wolf-killing
        behaviour. Known side effect: after a bubble death, the player respawns
        in the room opposite the entry side, for example entering through the
        east door can respawn at the east door of the room west of the
        cauldron. This is likely original death/respawn transition state and
        should be treated as a separate probe, not a failure of the bubble gate.
        Startup hardening, 2026 07 18: because restoring background id 0x13
        makes the original cauldron expansion live again, the emulator now
        starts paused, waits for the Knight Lore .z80 snapshot to finish
        loading, injects the Stage 7 physical cross first, then restores/zeros
        the Stage 8C background and bubble templates, and only then starts
        execution. This avoids a startup leak where early writes could be made
        before the snapshot load completed and then be overwritten by the
        original unvirtualized map.
     Built-in logical origin rule:
        Room (0,0) is authored to keep the original room 0x88 eight-block frame
        while leaving the center clear:
           type 0x00 at (2,6,0), (1,5,0), (5,6,0), (6,5,0),
                       (6,2,0), (5,1,0), (1,2,0), (2,1,0).

Working memory: between 5BA0h and 6107h

Live graphic object table:
     Starts at 0x5C08, with up to 40 slots of 0x20 bytes each.
     Slot layout, from the disassembly:
        +0 graphic/sprite id
        +1, +2, +3: X, Y, Z
        +4, +5, +6: width/radius X, depth/radius Y, height
        +7 flags:
            bit 7 = vertical flip
            bit 6 = horizontal flip
            bit 5 = wipe, transient
            bit 4 = draw, transient
            bit 3 = auto-adjust near arches, player only
            bit 2 = moveable
            bit 1 = ignore in 3D calculations
            bit 0 = near arch, player only
        +8 room
        +9, +10, +11: dX, dY, dZ as signed bytes
        +13 per-object direction/state information
     Confirmed guard/wizard slots:
        0x60C8: guard slot. Coordinates and +7 flags are confirmed correct.
        0x6028 and 0x6048: second guard type, top and lower halves. Coordinates are confirmed correct.
            This guard walks east/west, so +7 bit 6 does not toggle; the apparent lack of hflip change is expected.
            The east/west visual side is represented by the sprite id/dX logic, not by hflip.
        Wizard in the cauldron room: coordinates are confirmed in the dynamic entries paired with background rows 8 and 9.
            The same +7 flag layout must be used for the wizard; if a row comes from compact comparison data,
            read the raw flag byte from offset +7.
     Direction/state note:
        For NSEW guard/wizard movement, +13 low two bits decode as:
            0 = west, 1 = north, 2 = east, 3 = south.

Note: portcullis: 0x08~0x09 west/east
      ball: 0xB2~0xB3
      ghost: 0x50~0x52

guard: head: 0x96: west, 0x97: east
       body: 0x90~0x95: west, 0x98~0x9D: east

items/charms: During the game, the player has to collect and bring to the mage
       14 objects, but these are seven charm types requested twice. The eighth
       object code in the nearby sprite range, 0x67, is an extra life/consumable
       and should not be treated as a quest charm. I have found this page:
       https://www.porotal.org/knightlore/
       which describes, from a player's perspective, where they can be found (and in
       which order they must be retrieved).
       In this document:
       https://www.icemark.com/dataformats/knightlore/index.html
       it seems they are stored starting at 0x6ff2 (until 0x7111), in groups of 9 bytes.
       The screen (room?) where they are located is coded in offset 04, their current position
       is in 05, 06 and 07.
       -> The items in the table 0x6ff2 are updated once the player LEAVES the room leaving the
       item behind, not while the player is in the room toying around.
item codes:
       0x60: diamond/gem
       0x61: poison
       0x62: shoe/boot
       0x63: chalice/vase
       0x64: cup
       0x65: bottle
       0x66: ball/crystal ball
       0x67: life / extra life, not a quest charm
item locations in working memory (only 2):
1st:   0x5C48 -> sprite/object ID 1
       0x5C49 ~ 0x5C4B -> XYZ
       0x5C50 -> room ID
2nd:   0x5C68 -> sprite/object ID 2
       0x5C69 ~ 0x5C6B -> X, Y, Z
       0x5C70 -> room ID only for item/charm-like live objects. In the
       cauldron bubble/display slot this byte is state/sprite-like instead:
       the room-gated cauldron bubble probe observed 0xB4 here while the
       current physical room remained 0x88.
Stage 8.4 C3.0/C3.0b/C3.0c diagnostics:
       The runtime samples these values without writing item/charm/cauldron
       gameplay bytes, except for the explicit C3.0c cauldron acceptance gate
       noted below:
       - 0x5BB3: pickup_drop_pressed.
       - 0x5BB4: objects_carried_changed.
       - 0x5BBB: objects_put_in_cauldron.
       - 0x5BC3: all_objs_in_cauldron.
       - 0x5BC4: obj_dropping_into_cauldron.
       - 0x5BD8..0x5BE7: inventory handoff plus three carried-object display
         slots, 4 bytes each.
       - 0x5C48..0x5C67: first live item/charm candidate slot.
       - 0x5C68..0x5C87: second live item/charm/bubble/display candidate slot.
       - 0x5C88 + n * 0x20: first 8 bytes of expanded dynamic room/object
         records, currently n=0..7.
       - 0x6FF2..0x7111: 32 raw original static object-table records,
         9 bytes each. A direct read of the Knight Lore .z80 snapshot shows
         32 non-zero records in this span; gameplay-level object counts may
         still classify one or more records as special, duplicate, inactive,
         or otherwise not a normal visible collectible.
       The C3.0 table is meant for manual comparison while entering cauldron
       and charm rooms, picking up/dropping items in later probes, and watching
       whether leaving a room writes object state back into the static table.
       Live-slot rows also list all non-zero offsets in the sampled 32-byte
       slot. This is important because the visible byte preview may show the
       first 16 bytes as zero while a later byte still makes the slot non-empty.
       C3.0b also decodes live-slot pointer bytes +16/+17. For real special
       objects these should point back into 0x6FF2..0x7111.
       Cauldron caveat: the original drop code checks whether the player's
       physical room is 0x88. Under the Stage 7 sliding-center model, that can
       be true for any logical center room, so C3.1 must gate or patch original
       cauldron acceptance before allowing real item drops.
       C3.0c cauldron acceptance gate:
          The original handle_pickup_drop routine has two relevant hard-coded
          CP 0x88 checks:
             0xC086 FE 88, immediate byte at 0xC087:
                drop-slot scan limiter for the cauldron room.
             0xC0C9 FE 88, immediate byte at 0xC0CA:
                arm object-dropping-into-cauldron when room is 0x88 and
                player Z is high enough.
          The runtime verifies the opcode byte is still 0xFE, then changes only
          the immediate byte. In logical quest cauldron rooms, both immediates
          are restored to 0x88. Outside logical quest cauldron rooms, they are
          changed to 0xFF so the original physical-room comparison cannot match
          the sliding center room 0x88.
          This is still a probe/gate for original acceptance logic; it does not
          inject actual charms, change inventory bytes, or complete quests.
       C3.1 disposable charm record probe:
          Enabled explicitly with ?stage84c31=1 or ?stage8c31=1.
          The runtime rewrites exactly one original static object-table record:
             record 31, 0x7109..0x7111.
          This record is used as a disposable test charm source. No inventory,
          charm-order, cauldron-completion, or JS quest-completion bytes are
          directly written by this probe.
          Active record shape:
             byte +0: sprite/object code, normally the explicit
                      requiredCharm.sprite value in 0x60..0x66. Older
                      metadata without a sprite field falls back to
                      0x60 + (requiredCharm.id % 7), keeping the value inside
                      the original seven-charm range.
             byte +1..+3: X/Y/Z position, currently 0x80/0x80/0x80.
             byte +4: current physical room id for the logical charm anchor
                      visible in the five-room cross.
             byte +5..+7: start X/Y/Z, currently the same 0x80/0x80/0x80.
             byte +8: start physical room id, same as byte +4.
          Inactive record shape:
             0x00 0x80 0x80 0x80 0xFF 0x80 0x80 0x80 0xFF
          The record is armed whenever a logical quest charm anchor is present
          in the current physical five-room cross, preferably the center if the
          player is already in the charm room. Otherwise it is set inactive.
          If the live inventory/carried queue at 0x5BD8..0x5BE7 points back to
          0x7109, the runtime skips static-record reseeding so a carried probe
          item is not clobbered by recentering.
          Disposable-probe caveat: if the player drops this probe charm in a
          non-cauldron room, pick it back up before leaving the room. Until C3.2
          understands writeback and carried state fully, leaving after such a
          drop may cause the single disposable static record to be rearmed or
          disabled for the next cross instead of preserving that dropped state.
       C3.2 carried/inventory state probe:
          Build marker:
             stage84-c32-carried-inventory-20260718-1
          This is still an observation probe. It does not directly write
          inventory bytes, charm-order bytes, cauldron-completion bytes, or
          JS quest-completion state.
          The probe decodes the original inventory/carried queue:
             0x5BD8..0x5BDB: inventory handoff;
             0x5BDC..0x5BDF: carried display 0;
             0x5BE0..0x5BE3: carried display 1;
             0x5BE4..0x5BE7: carried display 2.
          Each four-byte slot is decoded as:
             byte +0: displayed carried object sprite;
             byte +1: flags/state;
             byte +2/+3: pointer back into the 0x6FF2..0x7111 static object
                         table, little endian.
          For the C3.1 disposable charm, the key success signal is:
             carried slot sprite 0x60..0x66;
             pointer 0x7109 -> static object 31.
          After pickup, record 31 may show sprite byte 0x00 while bytes +4/+8
          still hold the physical room. That does not mean the carried identity
          is lost; during this probe the carried display slot and its 0x7109
          pointer are the authoritative original-engine evidence.
          C3.2 adds three diagnostics rows:
             C3.2 carried identity:
                selected carried slot, object sprite, object class, flags,
                pointer, source static record, and exact JS source charm when
                the source is the C3.1 disposable record.
             C3.2 carry stability:
                first/last logical coordinate seen while carrying, number of
                distinct logical rooms visited while carrying, and recenter
                transitions survived while the pointer remains stable.
             C3.2 current-sector match:
                compares the exact JS source charm and sprite class with the
                current sector's required charm metadata.
          Corrected charm-identity conclusion:
             The original game has seven charm types, 0x60..0x66, and asks for
             each type twice across the 14-object order. Therefore the carried
             sprite class is a valid charm-type identity for C3.3 matching.
             0x67 is life/extra life and must be excluded from quest charm
             matching.
       C3.3 drop-gated JS-side cauldron acceptance probe:
          Enabled explicitly with ?stage84c33=1 or ?stage8c33=1.
          This first acceptance proof is intentionally JavaScript-side and
          player-action gated. It does not write original inventory bytes,
          charm-order bytes, original cauldron-completion bytes, or original
          global quest counters.
          Acceptance condition:
             current logical room role is cauldron;
             current sector has a quest and is not already completed;
             the carried queue at 0x5BD8..0x5BE7 contains a quest charm sprite
             in 0x60..0x66;
             the carried sprite equals the current sector's required charm
             sprite;
             the player is human or the form decode is unknown;
             a cauldron/drop signal is observed.
          The carried charm may come from any sector. Source sector is shown
          only for diagnostics; it is not part of the acceptance rule.
          Drop signal policy:
             strongest: obj_dropping_into_cauldron at 0x5BC4 changes from zero;
             strongest: objects_put_in_cauldron at 0x5BBB changes;
             fallback: pickup_drop_pressed at 0x5BB3 has a rising edge while
             at the logical cauldron with the matching carried charm and
             cant_drop at 0x5BD3 is zero.
          The diagnostics counter labelled "ready drop events" only counts
          drop-like edges observed while this ready condition is true. This
          avoids counting unrelated pickup/drop edges from earlier charm pickup
          or ordinary item handling.
          On acceptance the runtime marks the external persistent quest-sector
          state as completed and records the charm/source summary there. With
          C3.3 alone, the original carried object is not consumed by the JS
          state update. With C3.4 enabled, original cauldron acceptance may
          consume the carried object itself.
          The original requested-object cauldron display is independent unless
          the later C3.4 display/order patch is explicitly enabled.
       C3.4 original cauldron request-order patch:
          Enabled explicitly with ?stage84c34=1 or ?stage8c34=1.
          This probe uses the original request path discovered in the
          disassembly:
             objects_put_in_cauldron at 0x5BBB is the current request index;
             objects_required at 0xC27D..0xC28A is the mutable 14-byte request
             table;
             ret_next_obj_required returns objects_required[0x5BBB].
          In a logical quest cauldron room, or while standing in a directly
          reachable adjacent room before entering that cauldron, the runtime
          patches exactly one request table byte:
             address = 0xC27D + objects_put_in_cauldron;
             value = current JS sector required charm index 0..6.
          If the target JS quest sector is already completed, C3.4 leaves the
          original request slot unchanged. This prevents a completed local
          cauldron from repeatedly advancing the original 14-object counter
          during this probe.
          This should make the original cauldron reveal/display ask for the
          same charm as the JS sector. It also affects original cauldron
          acceptance, because the original drop code uses the same lookup when
          comparing the object being put into the cauldron.
          Risk:
             a matching drop may increment original objects_put_in_cauldron and
             trigger original colour/sound feedback. This is useful evidence,
             but it also means the original global request counter may drift
             across dispersed sectors until a later probe virtualizes or resets
             it deliberately.
          Manual validation on 2026 07 20:
             In sector (0,0), the JS required charm was mapped to original
             index 3 / sprite 0x63 chalice/vase. C3.4 pre-armed the adjacent
             cauldron room, the cauldron requested the chalice/vase, and a
             human-form drop produced the original successful colour shift.
             objects_put_in_cauldron advanced from 0x00 to 0x01, the carried
             queue became empty, and the JS sector state became completed.
             Build stage84-c34-completed-guard-20260720-1 then confirmed the
             completed-sector guard: at objects_put 0x01 / slot 1, target
             completed yes, C3.4 left the active request slot unchanged.
          Remaining C3.4 policy risk:
             The guard prevents one completed local cauldron from repeatedly
             advancing the original counter. It does not yet decide whether
             the original 14-object counter should advance across many
             different dispersed sectors, be virtualized per sector, be reset
             after each sector, or be replaced by JS-side quest progression.
       C3.5a read-only animation/display timing probe:
          Build marker:
             stage84-c35c-request-hold-20260721-1.
          C3.5a does not patch animation counters, live slots, display bytes,
          or emulator speed. It only measures observed visual-state cadence.
          Primary candidate:
             0x5C68..0x5C87, the second live item/bubble/display slot.
          The probe classifies sprite byte +0 as:
             0xA0..0xA7: cauldron bubble / wolf-attack bubble frames;
             0xA8..0xAE: requested-object display flashes, corresponding to
             original request indices 0..6.
          The expected requested-object display sprite is computed as:
             0xA8 + required charm index.
          Diagnostics report:
             current live sprite and classification;
             frame/sample interval between sprite changes;
             frame/sample interval between requested-object flashes;
             recent sprite trail;
             context reset reason;
             supporting dynamic-record candidates in 0x5C88 + n * 0x20.
          This is the evidence-gathering step before C3.5b attempts any
          slowdown. Short intervals here confirm the symptom but are not yet a
          fix.
       C3.5b opt-in cauldron display slowdown probe:
          URL flags:
             ?stage84c35b=1 enables the experiment;
             ?stage84c35bhold=6 chooses the target visible hold length
             (clamped to 1..30 frames, default 6);
             ?stage84c35bbubbles=human additionally enables human-form
             0xA0..0xA3 bubble-frame holding.
          Failed first attempt, observed 2026 07 21:
             predecessor replay on 0xA0..0xA7 bubble phases made C3.5a report
             constant writes, no releases, a reversed/forced sprite trail, and
             wolf-form bubbles stopped killing the player.
          Safer revision:
             default mode writes only 0xA8..0xAE requested-object display
             sprites;
             optional human-bubble mode also writes 0xA0..0xA3, but only while
             the player is human;
             the write uses the same observed sprite, not the predecessor
             sprite;
             never write 0xA4..0xA7 wolf-attack bubble frames;
             pause all writes when the player is wolf, transforming, or
             unknown form;
             keep original room-gated bubble behaviour untouched.
          Cyclic hold correction, 2026 07 21:
             A row showing values such as "visible 231/6" means the holder
             released once and then failed to start a new hold cycle. The
             corrected probe now releases one sample, resets the target, and
             allows the next eligible sprite sample to start a fresh cycle.
          Motion-hold revision, 2026 07 21:
             Sprite-only holding did not perceptibly slow the visible bubble
             motion. Human-bubble mode now holds the first sampled +0 sprite,
             +3 Z, and +11 dZ values during each hold cycle. Request flashes
             remain sprite-only. This is still post-frame memory surgery, so
             it may not become the final policy if a cleaner routine-level
             patch is found.
          Human-bubble mode may make harmless cauldron bubbles more readable
          while preserving the original wolf attack path. It remains a probe:
          manual testing must confirm that wolf-form bubbles still kill and
          human-form drops still complete correctly.
       C3.5c opt-in routine-level bubble throttle probe:
          URL flags:
             ?stage84c35c=1 enables the probe;
             ?stage84c35cperiod=4 runs one original bubble update frame for
             every four frames by default (clamped to 2..12).
          Targets:
             0xB8E8: CALL dec_dZ_and_update_XYZ inside upd_160_to_163;
             0xB8EB: CALL next_graphic_no_mod_4 inside upd_160_to_163;
             0xB926: LD (IX+0),0xA0 inside upd_168_to_175, expected bytes
             DD 36 00 A0.
          The probe reads and stores the original CALL/reset bytes, then
          on selected human-form frames patches 0xB8E8 to JP 0xB916, the
          routine's normal set_wipe_and_draw_flags exit. This skips the
          motion, graphic advance, dZ reset, and request-display branch as one
          unit, while leaving 0xB8EB restored/original.
          When the live sprite is a requested-object display 0xA8..0xAE, the
          probe instead patches the 0xB926 reset instruction to NOP NOP NOP
          NOP on throttled frames so the requested charm remains visible for
          the same cadence as the bubble frames. On active frames, outside
          logical cauldron rooms, and whenever the player is not human, it
          restores the original CALL/reset bytes.
          This is still a runtime poke, not a source-code modification. It is
          meant to test whether throttling the original routine before it
          draws is a better timing lever than post-frame live-slot pokes.
          C3.5b is automatically suspended while C3.5c is enabled so the two
          experiments do not interfere.
          Manual validation, 2026 07 21:
             - The first routine-level NOP attempt proved that the right
               routine had been found, but produced irregular human-form
               bubble speeds.
             - The early-exit revision, 0xB8E8 -> JP 0xB916 on throttled
               human-form bubble frames, produced a readable bubble cadence.
             - The request-hold revision, 0xB926 -> NOP NOP NOP NOP on
               throttled 0xA8..0xAE request-display frames, made the requested
               charm stay visible long enough to be useful.
             - Wolf-form behaviour remained original: diagnostic bytes were
               restored and the bubbles still killed the wolf.
             - With the default period 4, the rhythm is usable but slightly
               slow. Period 3 is the next recommended tuning candidate for
               longer play tests.
          Remaining C3.5 risks:
             - Bubble-death respawn displacement is still a separate known
               issue. A bubble death can respawn the player in a neighbouring
               room related to the entry side.
             - The C3.5c row should always show original bytes outside logical
               cauldron rooms and in wolf form. If it does not, reload the
               snapshot before continuing the probe.
             - This timing patch has only been tested over short sessions. It
               should remain opt-in until multi-sector quest runs confirm that
               cauldron display, wolf hazards, drops, and sliding recentering
               continue to coexist.

wizard: 0x9F and 0x9E -> head
        0x90 ~ 0x95 -> body

guard: 0x90~0x95 is the lower part of the back of the guard and
       0x96 is the head, while
       0x97 is the front head, and
       0x98~0x9D are the front view of the legs.
       0x1E and 0x1F are, without doubt, the head of the guard
0x08 0x09 portcullis
0x10~0x15 legs ????
0x16 gargoyle
0xB4~0xB5 fire
0x07 is a regular block,
0x52 is a ghost (mark this for possible later usage), and
0x54 is a table,
0x78~0x7F Character death?
0x5C68 cauldron spell bubbles:
    0xA0..0xA3: "good" bubbles
    0xA4..0xA7: wolf-attack bubbles
    0xAE: item-display state, using the sphere marker
0x8D cauldron, 0x8E cauldron "contents"
0xA4~0xA7 repelling spell

0x02 and 0x03 are the arches,
0x0A to 0x0F: brick walls

0x04 and 0x05: wooden entrance
0x80 to 0x82: wooden walls
